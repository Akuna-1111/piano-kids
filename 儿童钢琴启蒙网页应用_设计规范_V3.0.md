# 儿童钢琴启蒙网页应用 — 设计规范

**版本：V3.0**  
**适用项目：儿童钢琴启蒙网页应用**  
**配套开发文档：`儿童钢琴启蒙网页应用_开发规划_V3.0.md`**  
**目标平台：iPad / 手机 / 桌面浏览器**  
**主要姿态：横屏优先**

> **文档性质：Design Constitution / 设计宪法**
>
> 本文件是整个项目的视觉、交互、体验、动效、声音、文案与 AI 开发的上位规则。任何页面、组件、皮肤或新功能，均不得只追求“更丰富”，必须先证明其对核心用户任务有价值。
>
> **总定位：不要把它设计成一个“儿童游戏网页”，而要把它设计成一件“数字儿童乐器”。**

---

# 0. Design Constitution｜设计宪法

## 0.1 Piano First｜钢琴第一

钢琴永远是产品第一视觉主体。所有文字、按钮、奖励、皮肤、装饰与模式入口，都不能抢夺琴键的视觉优先级。

进入练习页面后，用户第一眼应该知道：

> **这里可以弹琴。**

而不是：

> **这里有很多功能。**

## 0.2 Touch Before Text｜先触摸，后阅读

儿童第一次使用时，不应依赖阅读说明来完成第一次操作。

优先顺序：

```text
触摸 → 声音 → 视觉反馈 → 极短文字 → 说明
```

如果一个交互必须先阅读一段文字才能理解，优先重新设计交互。

## 0.3 One Decision｜一次只解决一个决定

一个场景尽量只有一个核心决策。

```text
进入练习 → 弹琴
选择歌曲 → 选歌
完成歌曲 → 继续 / 再练一次
解锁装扮 → 试弹 / 装备
```

不要让儿童在同一个场景同时处理歌曲、键盘档位、音色、皮肤、统计、勋章等多个任务。

## 0.4 Sound Is UI｜声音就是界面

在数字钢琴中，声音不是附加媒体，而是交互反馈。

一次完整输入应形成：

```text
触摸 → 按压反馈 → 发声 → 松开 → 状态恢复
```

视觉状态与声音时间轴必须一致。不能出现“按钮已经变化，声音还没有出现”的明显延迟感。

## 0.5 Quiet by Default｜默认安静

静态、安静、低干扰是默认状态。

只有真正发生事件时才产生视觉变化：

```text
普通状态 → 安静
按键 → 短反馈
正确音符 → 确认
完成歌曲 → 成长反馈
解锁装扮 → 仪式反馈
```

不让背景、图形和装饰持续运动。

## 0.6 Growth, Not Commerce｜成长，不是消费

成长点只表达练习成果，不设计成交易货币。

禁止使用：

```text
购买 / 价格 / 充值 / 折扣 / 抽卡 / 随机开箱 / 支付
```

推荐表达：

```text
获得成长点
成长进度 18 / 30
新的琴键装扮已解锁
```

## 0.7 Material Over Decoration｜材质优于装饰

儿童感不依靠贴纸、Emoji、卡通边框或大量图案，而来自：

```text
触感 + 比例 + 色彩 + 声音 + 空间 + 状态反馈
```

视觉成熟度必须高于年龄标签。

## 0.8 Meaning Before Motion｜先有语义，再有动效

任何动效都必须回答：

> **它告诉用户发生了什么？**

没有清晰语义的动画默认删除。

## 0.9 Delete Before Add｜先删，再加

当页面看起来“空”时，第一选择不是加元素，而是重新审查：

```text
比例 / 排版 / 留白 / 对齐 / 层级 / 内容顺序
```

只有确认结构已经成立，才考虑增加视觉元素。

---

# 1. Product Experience Thesis｜产品体验命题

## 1.1 本产品真正要解决的问题

儿童不应该“学习如何使用这个 App”，而应该直接进入“学习弹琴”本身。

因此核心体验链必须保持极短：

```text
打开
↓
看到钢琴
↓
碰一下
↓
听到声音
↓
产生下一步
↓
完成一次练习
↓
看到成长
↓
愿意再来
```

## 1.2 体验成功标准

产品成功不是功能数量，而是以下四件事是否同时成立：

1. 第一次打开不需要解释。
2. 第一次触摸立即得到声音和视觉反馈。
3. 练习期间不会被系统 UI 打断。
4. 完成之后产生清晰但不过度的成长感。

## 1.3 设计成熟度

最终观感应接近：

```text
一件高质量数字乐器
+
一位温和的启蒙老师
+
极少量但有效的游戏化成长
```

而不是：

```text
儿童手游
+
教育 Dashboard
+
AI 生成网页模板
```

---

# 2. User Model｜用户模型

## 2.1 儿童用户

儿童是主要操作者。主要行为特征：

- 阅读依赖较低，直觉操作依赖较高
- 注意力容易被持续动画打断
- 手指误触概率高于成人
- 对声音和按压反馈敏感
- 对“下一步”比对复杂说明更敏感
- 容易因为失败反馈产生放弃行为

因此设计目标是：

```text
低理解成本
低误触成本
即时反馈
低失败压力
明确下一步
可见成长
```

## 2.2 家长用户

家长主要判断：

```text
安全
可信
无广告
无诱导消费
无账号压力
隐私简单
真的适合练琴
```

因此家长感知到的产品气质应该是：

> **克制、专业、稳定、可信。**

---

# 3. Anti-AI Design｜反 AI 生成感

## 3.1 核心目标

“看起来不像 AI 生成”不是靠加入更多所谓高级视觉效果，而是靠人为做出明确取舍：

```text
少
准
稳
有比例
有秩序
有材质
```

## 3.2 全项目禁止 Emoji

**任何 UI、按钮、卡片、状态、奖励、皮肤、导航、空状态、数据字段都禁止使用 Emoji。**

禁止将 Unicode 彩色字符当作图标或视觉素材。

需要图形时必须使用：

```text
SVG / CSS Shape / 自绘 Icon / 受控图片资源
```

## 3.3 永久禁止的视觉套路

```text
紫蓝渐变
默认彩虹渐变
玻璃拟态
霓虹发光边框
大量粒子
无限漂浮
无意义旋转
AI 常见发光圆环
全部模块卡片化
大量相同圆角卡片
所有组件都带阴影
游戏商城式 Badge 墙
无意义数据 Dashboard
```

## 3.4 不通过“儿童化”制造儿童产品

禁止把儿童产品自动解释成：

```text
卡通乐园
贴纸合集
糖果 UI
泡泡字
动物头像大集合
彩色游戏商城
```

儿童年龄较小，不代表视觉成熟度也要低。

## 3.5 高级感来源

本产品的高级感只能主要来自：

```text
排版
比例
留白
材质
边界
微妙状态
声音
一致性
```

而不是来自：

```text
渐变
发光
模糊
3D
大量动画
```

---

# 4. Visual Hierarchy｜视觉优先级

所有页面固定遵循：

```text
一级：当前要做什么
二级：下一步做什么
三级：辅助信息
四级：装饰
```

装饰永远不得超过当前任务的重要程度。

例如练习页面：

```text
当前要弹什么      一级
钢琴键盘          一级
开始 / 暂停        二级
歌曲进度          二级
成长数据          三级
装饰              四级
```

---

# 5. Interaction Hierarchy｜交互优先级

优先顺序：

```text
直接操作琴键
↓
点击主操作
↓
简单选择
↓
进入菜单
```

能直接操作完成的事情，不要强迫用户通过菜单完成。

---

# 6. Design Decision Framework｜设计决策框架

以后增加任何功能、页面或组件，都必须先回答以下问题：

```text
1. 它服务谁？
2. 它解决什么任务？
3. 为什么现在需要出现？
4. 它是否抢夺钢琴注意力？
5. 它能不能被删除？
6. 它能不能使用已有组件完成？
7. 它是否真的需要动画？
8. 它是否真的需要独立页面？
```

如果回答不清楚：

> **不进入开发。**

---

# 7. 信息层级原则

整个产品的视觉层级固定为：

```text
一级：当前要做什么
二级：下一步做什么
三级：辅助信息
四级：装饰
```

任何页面都不能让装饰层级高于操作层级。

例如练琴页面：

```text
歌曲标题             一级
下一音符 / 引导       一级
钢琴键盘             一级
播放控制             二级
准确率               二级
星星 / 成长           三级
装饰                  四级
```

---

# 8. Layout 版式系统

## 4.1 整体布局原则

采用“舞台 + 控制层”的结构，而不是传统后台式栅格。

```text
┌────────────────────────────────────────────┐
│ 顶部轻量状态区                             │
├────────────────────────────────────────────┤
│                                            │
│                音乐内容舞台                │
│                                            │
│         歌曲 / 引导 / 状态反馈             │
│                                            │
├────────────────────────────────────────────┤
│                                            │
│                 钢琴键盘                   │
│                                            │
└────────────────────────────────────────────┘
```

钢琴是页面的核心主体。

不要把钢琴压缩成页面中的一个普通组件。

## 4.2 8pt 空间系统

统一使用 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 的空间级数。

推荐：

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 24px;
--space-6: 32px;
--space-7: 48px;
--space-8: 64px;
```

不要出现大量随机值：

```css
17px
19px
23px
27px
31px
```

除非用于特殊视觉校准。

## 4.3 横屏优先

### iPad 横屏

优先保证：

- 键盘高度充足
- 所有主要操作单手/双手容易触达
- 页面不出现滚动条
- 不产生横向拖拽
- 不需要缩放
- 不需要左右滑动寻找琴键

### 手机横屏

优先缩减辅助内容，而不是缩小钢琴键到不可操作。

基本原则：

> **先删内容，再缩尺寸。**

## 4.4 竖屏

竖屏不是主设计态。

允许采用简化布局：

```text
歌曲信息
↓
主要控制
↓
琴键
↓
少量辅助操作
```

不要求完整展示所有复杂模块。

---

# 9. 色彩系统

## 5.1 基础 UI

基础界面采用明亮、中性、温暖的浅色体系。

推荐基准：

```css
--color-bg: #F7F7F3;
--color-surface: #FFFFFF;
--color-surface-soft: #F1F2EE;
--color-text: #252824;
--color-text-secondary: #6F746D;
--color-text-tertiary: #9A9F97;
--color-border: #E3E5DF;
--color-border-strong: #D0D4CC;
--color-accent: #4C8A67;
--color-accent-soft: #E5F0E8;
--color-success: #4D8A62;
--color-warning: #B98232;
--color-danger: #B4574D;
```

说明：以上颜色不是最终品牌色，而是 V1 视觉基准。开发过程中不得擅自引入新的主色体系。

## 5.2 色彩数量控制

单个页面常驻视觉主色不超过：

- 1 个品牌强调色
- 1 个状态辅助色
- 中性色体系

儿童产品可以有颜色，但颜色必须有语义。

## 5.3 高饱和色的使用

高饱和色仅用于：

- 当前按键
- 下一键提示
- 成功反馈
- 解锁瞬间
- 节拍命中反馈

不得大面积铺满界面。

---

# 10. Typography 字体规范

## 6.1 中文字体

优先使用系统字体栈：

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  "PingFang SC",
  "Microsoft YaHei",
  "Noto Sans SC",
  sans-serif;
```

不主动加载大体积中文 Web Font。

## 6.2 数字与英文

数字与英文可以使用系统无衬线字体，重点是保证数字清晰、间距稳定。

## 6.3 字号层级

建议基准：

```text
48–56px    页面/歌曲核心标题
32–40px    主要状态数字
24–28px    页面区块标题
18–20px    主操作文字
16px       常规文字
14px       辅助文字
12px       极少量说明文字
```

儿童界面不能依赖 11px、10px 等极小字体传达重要信息。

## 6.4 字重

推荐只使用：

- 400 Regular
- 500 Medium
- 600 Semibold

避免页面出现大量 700 / 800 / 900 粗体。

## 6.5 排版原则

不要所有内容都居中。

儿童产品不代表所有文字都应该居中。

推荐：

- 核心标题可以居中
- 操作说明尽量左对齐
- 列表信息左对齐
- 数值可以右对齐
- 按钮文字居中

---

# 11. Corner / Border / Shadow

## 7.1 圆角

圆角需要体现“柔和”，但不能变成“气泡 UI”。

推荐：

```css
--radius-sm: 8px;
--radius-md: 12px;
--radius-lg: 16px;
--radius-xl: 20px;
```

大型容器最多使用 20px 左右。

不要所有元素都使用 999px。

## 7.2 Border

边框是主要的秩序工具之一。

优先使用低对比度 1px 边框，而不是阴影制造层次。

## 7.3 Shadow

阴影应该像真实物体产生的接触阴影。

推荐：

```css
box-shadow: 0 1px 2px rgba(25, 30, 25, 0.05);
```

需要强调悬浮层时才使用更明显的阴影。

禁止：

- 大面积黑色阴影
- 彩色发光阴影
- 高斯模糊式彩色光晕
- 卡片连续叠加多个 shadow

---

# 12. Icon 图标规范

## 8.1 图标来源

统一使用：

- SVG
- CSS 图形
- 项目自绘 Icon

禁止使用：

- Emoji
- 字符号代替图标
- 图片中的文字图标
- 不同风格 Icon 混用

## 8.2 Icon 风格

推荐：

- 单色
- 线性
- 2px 左右视觉线宽
- 几何简洁
- 端点圆润

图标尺寸：

```text
16px    极少量辅助操作
20px    常规按钮
24px    主要控制
28–32px 特殊核心控制
```

## 8.3 图标不是装饰

每一个图标都必须表达一个明确动作或状态。

不要因为“空”而放图标。

---

# 13. Button 按钮系统

## 9.1 核心按钮原则

按钮必须让孩子看到后立即知道：

> “按这里就可以继续。”

核心按钮优先：

- 大
- 简单
- 高对比
- 文案少
- 图标辅助

## 9.2 主按钮

一次页面只允许一个最强主按钮。

例如：

```text
开始跟弹
```

其它操作弱化。

## 9.3 禁止按钮堆叠

不要出现：

```text
开始练习
听一遍
查看谱子
调整速度
选择音色
查看记录
更多设置
```

全部同时出现。

应该建立主任务优先级。

---

# 14. Piano Keyboard 键盘视觉规范

钢琴是整个产品最重要的 UI，不应使用普通网页 Button 的视觉逻辑直接实现。

## 10.1 键盘应该像“真实乐器”

目标不是“很多白色按钮”，而是：

> **屏幕上的一件小型数字乐器。**

需要具备：

- 明确的键体层次
- 白键与黑键关系
- 接触阴影
- 按压反馈
- 音名标识
- 当前手指位置反馈

## 10.2 白键

白键是视觉基准。

推荐：

- 非纯白底色
- 极轻微纵向明暗变化可以使用，但不要做明显玻璃渐变
- 1px 边界
- 底部轻微接触阴影
- 按下后产生真实位移感

## 10.3 黑键

黑键必须采用独立层。

禁止用简单的 `flex` 与白键混排假装标准钢琴键位。

推荐架构：

```text
Keyboard
├── WhiteKeyLayer
└── BlackKeyLayer
```

黑键通过绝对定位计算位置。

## 10.4 按下状态

按键按下时优先使用：

```text
translateY
shadow reduction
background shift
```

而不是：

```text
glow
blur
scale 变形
```

目标是让孩子感受到“我按下去了”。

## 10.5 引导状态

下一键提示是练习模式最重要的视觉反馈。

推荐三种状态层级：

### 一级提示

轻量外轮廓 / 明确高亮。

### 二级提示

配合轻微呼吸动画。

### 三级提示

如果孩子长时间没有操作，再增加辅助方向提示。

禁止使用不停闪烁、强发光、快速缩放等会制造焦虑的提示。

---

# 15. 皮肤系统设计规范

皮肤系统必须是“钢琴外观系统”，而不是“整个网页换皮系统”。

## 11.1 皮肤影响范围

允许影响：

- 琴键
- 琴键按下态
- 琴键提示态
- 钢琴底座
- 少量舞台背景
- 少量装饰线条

不允许影响：

- 主要导航结构
- 核心按钮尺寸
- 页面栅格
- 文字层级
- 交互逻辑
- 可访问性

## 11.2 皮肤风格

皮肤不是“颜色主题”。

应该围绕一种视觉材料或空间氛围展开，例如：

```text
自然木质
纸张
海洋
夜空
森林
糖果
晶体
节日
```

但所有皮肤必须经过同一套设计质量控制。

## 11.3 皮肤禁止 Emoji

皮肤中的装饰必须使用：

- SVG
- CSS Shape
- 几何图形
- 简单线稿
- 轻量 WebP / SVG

禁止使用 Emoji 作为装饰素材。

例如：

错误：

```text
太空 + 星球 Emoji + 火箭 Emoji
```

正确：

```text
太空
深蓝背景
细线轨道
几何行星
少量微小星点
```

## 11.4 皮肤强度

皮肤必须控制在“看得出来，但不打扰演奏”的范围。

建议：

```text
70% 基础琴键结构
20% 皮肤材质
10% 装饰
```

---

# 16. “我的装扮”页面

不使用“商店”语言。

推荐名称：

> **我的装扮**

页面重点是：

> 看见 → 试弹 → 解锁 → 装备

而不是：

> 浏览 → 购买 → 消费

## 12.1 页面布局

不要采用普通电商商品卡片布局。

推荐采用横向展示的“琴键样品板”：

```text
        我的装扮

当前星星成长值                  12

┌─────────┐ ┌─────────┐ ┌─────────┐
│         │ │         │ │         │
│ 琴键样品 │ │ 琴键样品 │ │ 琴键样品 │
│         │ │         │ │         │
└─────────┘ └─────────┘ └─────────┘

      点击任意样品即可试弹
```

## 12.2 未解锁状态

不要简单使用灰度滤镜 + 锁图标。

应该保持皮肤部分可见，让孩子理解：

> “它还没有得到，但我能看到它长什么样。”

解锁信息只需要一句话：

```text
还需要 3 次练习
```

或：

```text
完成《小星星》后获得
```

## 12.3 解锁反馈

解锁时只进行一次完整反馈：

```text
状态变化
↓
轻微视觉展开
↓
声音确认
↓
“新装扮已获得”
```

不要：

- 满屏烟花
- 大量粒子
- 大量闪光
- 震动式动画
- 连续弹窗

---

# 17. 动效规范

## 13.1 动效目标

动效的职责不是展示技术，而是解释状态。

设计原则：

> **静态是默认，动效是反馈。**

## 13.2 动效时长

推荐：

```text
80–120ms     按键反馈
120–180ms    按钮状态
180–260ms    小面板出现
250–400ms    页面状态切换
400–700ms    成长/解锁反馈
```

避免所有动画都使用 500ms / 800ms / 1000ms。

## 13.3 Easing

推荐：

```css
cubic-bezier(0.2, 0.8, 0.2, 1)
```

按键反馈推荐更快的 ease-out。

## 13.4 Reduced Motion

必须支持：

```css
@media (prefers-reduced-motion: reduce)
```

减少或取消非必要动画。

---

# 18. 音效与视觉反馈关系

视觉反馈必须和声音反馈同步。

例如按键：

```text
手指接触
↓
视觉下压
↓
音符发声
↓
手指抬起
↓
视觉恢复
```

禁止出现：

```text
先动画
500ms 后才出声音
```

或：

```text
音乐已经停止
界面仍然持续大量动画
```

音乐类产品必须让“声音时间轴”和“视觉时间轴”一致。

---

# 19. 练琴页面 UX 规范

练琴页面是核心页面，必须尽可能减少选择。

## 15.1 进入页面

推荐结构：

```text
歌曲名
简单难度提示

[ 听一遍 ]

钢琴

[ 开始跟弹 ]
```

避免在开始前让儿童选择过多参数。

## 15.2 跟弹过程中

屏幕最重要的只有：

```text
当前要弹什么
↓
我弹对了吗
↓
下一步是什么
```

不要持续显示大量统计数字。

## 15.3 错误反馈

错误不要使用红色弹窗。

推荐：

- 当前键轻微反馈
- 正确键继续等待
- 节奏不中断
- 文案不责备

产品语言应该传递：

> 继续就好。

而不是：

> 错了。

---

# 20. 评分页面 UX 规范

评分结果的第一重点是“完成”，第二重点才是“准确”。

推荐视觉顺序：

```text
完成
↓
表现反馈
↓
成长奖励
↓
下一步
```

避免把准确率做成竞技分数。

例如不建议：

```text
97.42 分
排名第 3
超越 85% 玩家
```

推荐：

```text
这首歌完成了
准确度 96%
获得 3 个星星成长值
```

---

# 21. 我的成长页面

成长页面不是数据后台。

不制作：

- 雷达图
- 折线图
- 饼图
- 排行榜
- 复杂统计卡

推荐：

```text
已经完成 12 首歌

连续练习 4 天

最佳准确度 96%

已经获得 5 套装扮
```

用数字 + 简短说明表达即可。

---

# 22. 空状态规范

空状态必须友好、安静、有下一步。

例如没有练习记录：

```text
还没有练习记录

弹一首简单的歌试试

[ 开始第一首 ]
```

禁止用复杂插画填满整个屏幕。

---

# 23. Loading / 初始化规范

本项目不应该出现明显的传统“加载页”。

因为产品定位是“打开即玩”。

优先策略：

```text
页面先出现
↓
钢琴结构先出现
↓
音频准备完成
↓
可操作
```

加载中只使用轻量状态提示，例如：

```text
准备钢琴…
```

不使用：

- 旋转大 Loading
- 品牌 Logo 长时间动画
- 全屏渐变背景
- 进度条假加载

---

# 24. Responsive 响应式规范

设计不能依赖单一屏幕尺寸。

## 20.1 断点策略

不根据设备品牌判断，而根据可用空间判断。

建议基准：

```text
>= 1024px     iPad 横屏 / 桌面
768–1023px    小平板
480–767px     手机横屏
< 480px       手机竖屏
```

## 20.2 键盘尺寸优先级

响应式缩放顺序：

```text
先保持键盘可操作面积
↓
减少顶部信息
↓
减少辅助按钮
↓
缩减装饰
↓
最后才压缩键帽
```

禁止因为保持“视觉比例”而让键盘变得难以点击。

---

# 25. Touch Interaction 触控规范

儿童触控需要比普通 Web 更宽松。

目标触控区域建议：

```text
核心操作 ≥ 44px
主要按钮 ≥ 48px
钢琴键宽度根据屏幕动态计算
```

重点不是遵守某一个数字，而是保证儿童手指操作的容错率。

必须优先使用：

```text
Pointer Events
setPointerCapture()
touch-action
```

避免依赖单纯 mouse 事件。

---

# 26. Accessibility 无障碍与可读性

儿童产品仍然需要基本可访问性。

必须满足：

- 键盘有 aria-label
- 控制按钮有明确名称
- 不能只依靠颜色表达状态
- 高亮必须同时有形状/边框/位置变化
- 文字和背景保持足够对比度
- reduced motion 可用
- focus 状态明确

色盲情况下也必须能判断：

```text
正确
下一键
已经按下
尚未解锁
```

---

# 27. 文案规范

## 23.1 语言风格

文案要像一个温和的音乐老师，而不是游戏运营团队。

推荐：

```text
开始弹
再试一次
继续练
听一遍
已经完成
做得不错
再弹一次
```

避免：

```text
超级大奖
史诗奖励
SSR
限时抢购
立即领取
冲刺排行
击败其他人
```

## 23.2 奖励语言

强调“获得”，不强调“消费”。

推荐：

```text
获得新的装扮
```

而不是：

```text
花 10 颗星购买
```

因为星星是成长值，不是消费货币。

---

# 28. 状态设计

所有组件至少考虑以下状态：

```text
Default
Hover
Pressed
Focus
Disabled
Active
Selected
Locked
Completed
Loading
Error
```

触摸设备上重点设计：

```text
Default
Pressed
Active
Selected
Locked
Completed
```

不能只设计一个漂亮的 Default 截图。

---

# 29. AI 开发专用规则

本文件同时作为 Claude Code / DeepSeek Harness 的视觉约束文件。

开发 AI 必须遵守：

## 25.1 不得自行设计新视觉体系

如果需求没有明确颜色、圆角、阴影、动画等参数：

> 优先复用现有 Design Tokens，而不是自行生成一套新的视觉样式。

## 25.2 不得自行增加 UI

不要因为页面“看起来空”而增加：

- 卡片
- Banner
- Icon
- 插画
- 数据统计
- 快捷入口
- Tooltip
- 装饰图形

留白本身就是设计。

## 25.3 不得为了“高级感”加入视觉效果

高级感来源：

```text
准确的尺寸
准确的间距
准确的比例
准确的字体
准确的对齐
准确的状态
```

不是：

```text
渐变
发光
模糊
粒子
玻璃
3D
```

## 25.4 不得把页面做成 Dashboard

除非明确要求，否则不要出现：

```text
Card Grid
Stats Cards
Sidebar Dashboard
Analytics Panel
```

首页应该是一个“乐器入口”，不是后台系统。

## 25.5 不得自动加入 Emoji

再次强调：

> **项目代码、数据、文案、按钮、装饰、皮肤、空状态均禁止 Emoji。**

需要图标时使用 SVG Icon。

## 25.6 不得使用默认 Tailwind / shadcn 风格

如果使用 Tailwind：

- 必须使用项目 Design Tokens
- 不得使用默认组件外观
- 不得直接套用 shadcn 卡片、按钮、Dialog 的默认风格

最终视觉需要表现为一个完整产品，而不是组件库 Demo。

---

# 30. Anti-AI Visual QA

每完成一个页面，都必须进行以下人工/AI 自检。

回答以下问题：

### Q1
如果去掉所有插画、图标和颜色，这个页面仍然有好的版式吗？

### Q2
是否存在大量大小相同的卡片？

### Q3
是否所有模块都拥有相同的圆角和阴影？

### Q4
是否出现没有实际意义的装饰？

### Q5
是否出现 AI 常见紫蓝渐变？

### Q6
是否使用 Emoji 充当 Icon？

### Q7
孩子能否在 2 秒内知道下一步做什么？

### Q8
家长看到页面时是否会觉得像一个真正的产品，而非 Demo？

### Q9
核心钢琴是否仍然是页面视觉中心？

### Q10
删掉其中 20% 的 UI 后，页面是否反而更好？

如果多个答案不理想，应优先**删减 UI**，而不是继续增加装饰。

---

# 31. 页面级视觉模板

## 27.1 首页

关键词：

> 安静、直接、音乐入口。

结构：

```text
品牌 / 简洁状态

今天想弹什么？

[ 跟我弹 ]   [ 自由弹 ]

最近练习

──────────

装扮 / 成长 / 设置
```

## 27.2 跟弹页

关键词：

> 专注、清晰、节奏感。

结构：

```text
歌曲名称
轻量进度

              当前音符

─────────────────

            钢琴

─────────────────
```

## 27.3 我的装扮

关键词：

> 展示、试弹、收集。

不要做传统商城。

## 27.4 成长

关键词：

> 成就感，但不制造竞技压力。

## 27.5 挑战

关键词：

> 单一目标、即时反馈、短时间完成。

---

# 32. 组件设计原则

所有核心组件遵循：

```text
结构稳定
样式可变
状态明确
数据驱动
无隐藏副作用
```

建议组件层级：

```text
AppShell
├── TopBar
├── Stage
│   ├── SongHeader
│   ├── PracticeGuide
│   └── ResultFeedback
├── PianoKeyboard
│   ├── WhiteKeyLayer
│   └── BlackKeyLayer
└── BottomControls
```

皮肤只注入 Token，不改变组件结构。

---

# 33. 设计 Token 原则

所有视觉参数集中管理。

建议：

```text
src/styles/tokens.css
src/styles/themes.css
src/styles/motion.css
src/styles/components.css
```

组件尽量只调用语义 Token：

```css
background: var(--color-surface);
color: var(--color-text);
border-color: var(--color-border);
border-radius: var(--radius-md);
```

而不是在组件里大量出现硬编码值。

禁止这种代码大量扩散：

```css
background: #f6f6f6;
border-radius: 13px;
box-shadow: 0 6px 22px rgba(...);
```

---

# 34. Design Review 最终验收标准

一个页面只有同时满足以下条件，才可以认为设计完成：

### 视觉

- 没有 Emoji
- 没有无意义渐变
- 没有过量阴影
- 没有无意义动画
- 没有 AI 模板式卡片堆叠
- 没有视觉噪音
- 页面留白充足
- 对齐关系清晰
- 字体层级明确

### UX

- 儿童能快速理解当前任务
- 核心按钮明显
- 误触成本低
- 错误不会造成压力
- 操作反馈即时
- 页面不会产生横向拖拽
- 钢琴始终是视觉主体

### 产品气质

最终效果应该接近：

> **一件经过工业设计的数字儿童乐器，而不是一个儿童游戏网页。**

---

# 35. 给 AI 编程模型的最终指令

在修改任何 UI 之前，先阅读：

```text
儿童钢琴启蒙网页应用_开发规划_V3.0.md
儿童钢琴启蒙网页应用_设计规范_V3.0.md
```

两份文件共同构成开发约束。

当需求与设计规范冲突时：

1. 优先保证核心钢琴操作。
2. 优先保证清晰度与触控体验。
3. 优先复用已有 Design Tokens。
4. 优先删减，而不是增加 UI。
5. 不得使用 Emoji。
6. 不得自行生成 AI 风格的渐变、玻璃、霓虹、粒子或卡片墙。
7. 不得为了“高级感”加入不必要的视觉效果。
8. 不得改变现有信息架构，除非需求明确要求。
9. 不得引入无法解释用途的组件。
10. 每完成一个页面，必须通过 Anti-AI Visual QA。

最终目标不是“页面看起来很丰富”，而是：

> **少一点，但每一个细节都像经过专业设计。**

---

# 36. V3.0 Design Constitution｜新增最高级设计规则

V3.0 不再把“专业审美”理解成颜色、圆角、阴影和动效的组合，而是把它定义为：

> **通过极少的设计语言，让用户快速理解、自然操作，并长期使用。**

因此，任何新设计必须同时满足：

```text
功能正确
+
认知清晰
+
触控自然
+
反馈及时
+
视觉克制
+
系统一致
```

如果一个设计只满足“看起来漂亮”，而没有提升理解与操作效率，则不进入正式版本。

---

# 37. User Truth｜从真实用户出发

设计评审不能从“设计师想做什么”开始，而应从“用户实际遇到什么”开始。

儿童第一次打开产品时，不应知道：

```text
这里有几个模块
这里用了什么设计风格
这里有哪些奖励
```

儿童只需要知道：

```text
我可以按这里
这里会发出声音
接下来我应该按哪里
我完成了
我可以继续
```

家长第一次看到产品时，也不应该关注：

```text
视觉有多炫
动画有多少
功能有多丰富
```

而应快速感知：

```text
这是安全的
这是克制的
这是专业的
这是真的可以练琴的
```

---

# 38. Design Tension｜设计中的五组矛盾

本项目必须主动处理以下矛盾，而不是试图全部最大化。

## 38.1 儿童感 vs 专业感

儿童感来自触觉、声音、比例与温度，不来自卡通堆砌。

## 38.2 游戏性 vs 学习性

可以提供探索和奖励，但不能让奖励成为主任务。

## 38.3 丰富性 vs 安静

功能可以丰富，但同一时刻可见信息必须少。

## 38.4 个性化 vs 系统一致

皮肤可以变化，但产品骨架不能变化。

## 38.5 即时反馈 vs 注意力保护

反馈必须立即出现，但不能持续刺激。

设计目标不是消灭这些矛盾，而是在每个场景中选择明确的平衡点。

---

# 39. Attention Budget｜注意力预算

儿童的注意力是一种有限资源。

因此每一个页面必须控制同时争夺注意力的对象数量。

建议：

```text
1 个主要对象
1 个当前任务
最多 2 个辅助操作
```

超过这个数量时，应重新审查信息架构。

例如练琴页面：

```text
主要对象：琴键
当前任务：下一音符
辅助操作：暂停 / 返回
```

而不是：

```text
琴键
歌词
星星
勋章
皮肤
节拍
排行
提示
广告式推荐
```

---

# 40. Visual Silence｜视觉静默

界面的高级感必须允许“没有东西发生”。

静止不是空洞，而是给用户建立秩序和专注的时间。

以下状态默认保持静态：

```text
首页等待
选歌浏览
装扮浏览
练习准备
设置
```

只有事件发生时才出现动态变化。

---

# 41. Visual Rhythm｜视觉节奏

页面也应该像音乐一样拥有节奏：

```text
稳定
→ 变化
→ 确认
→ 回归稳定
```

禁止：

```text
变化
→ 变化
→ 变化
→ 变化
```

永远不停止的视觉变化会降低真正反馈的价值。

---

# 42. Tactile Language｜触感语言

琴键交互应该形成产品独有的触感语言。

建议固定为：

```text
接触：短
下压：轻
声音：即时
保持：稳定
释放：自然
```

任何模式都应尽量继承这种节奏。

不要因为切换到不同模式，就让琴键出现完全不同的物理行为。

---

# 43. Learning Feedback Ladder｜学习反馈层级

反馈必须有等级。

## L0：无反馈

表示：没有需要强调的事件。

## L1：确认

表示：操作已经发生。

## L2：引导

表示：用户下一步可以做什么。

## L3：鼓励

表示：阶段性成果已经发生。

## L4：成长

表示：长期进步被记录。

不同反馈层级必须使用不同视觉强度。

不能把每一次按键都做成 L3 或 L4。

---

# 44. Error Without Shame｜无羞耻错误机制

错误设计必须帮助孩子继续学习，而不是证明孩子做错了。

推荐状态链：

```text
错误
→
轻提示
→
继续
```

不推荐：

```text
错误
→
失败画面
→
处罚
→
重来
```

尤其不要通过颜色、声音、震动、动画同时放大错误。

---

# 45. Reward Economy｜奖励经济学约束

奖励系统虽然不涉及金钱，但依然必须防止“过度游戏化”。

星星的意义应该是：

```text
成长被记录
```

而不是：

```text
资源被消耗
```

因此推荐：

```text
累计获得
→
达到成长节点
→
自动解锁
```

不推荐：

```text
累计获得
→
花掉
→
重新积累
```

这样可以减少儿童对“货币”的错误理解，也让练习成果具有持续积累价值。

---

# 46. Skin Is Material｜皮肤不是营销

皮肤设计应该像给真实乐器更换材质，而不是给游戏角色换服装。

因此设计方向更接近：

```text
木材
纸张
石材
金属
夜色
自然
```

而不是不断增加：

```text
角色
表情
贴图
贴纸
徽章
装饰符号
```

皮肤可以有个性，但不应该破坏乐器的身份。

---

# 47. Skin Contrast Rule｜皮肤对比规则

无论任何皮肤：

```text
音名可读
黑白键关系可辨
高亮状态明确
按下状态可见

``` 

皮肤绝不能为了“主题感”降低钢琴操作效率。

优先级永远是：

```text
可弹
>
可辨
>
可学
>
好看
```

---

# 48. No Marketing Surface｜禁止营销式表面

本产品不是电商，也不是内容平台。

禁止出现类似营销页面的模块：

```text
热门推荐
限时活动
人气排行
今日精选
立即领取
新品上线
更多好礼
```

除非未来产品需求明确引入相关内容，否则不得自行添加。

---

# 49. No Dashboard Thinking｜禁止后台式设计思维

即使产品内部存在大量数据，也不意味着用户需要看到数据墙。

后台数据的存在目的，是帮助：

```text
练习
反馈
成长
```

不是为了展示系统有多少统计能力。

例如：

不推荐：

```text
总弹奏次数
累计分钟
最长连续
历史趋势图
周统计
月统计
排名
```

全部同时出现。

推荐只在需要时展示一项最有意义的数据。

---

# 50. State Before Style｜先定义状态，再定义视觉

设计任何新组件时，顺序必须是：

```text
业务状态
→
用户意图
→
视觉状态
→
动画状态
```

例如皮肤卡片：

```text
locked
ready
unlocked
equipped
previewing
```

每个状态必须有明确视觉差异，但差异不能仅靠颜色实现。

---

# 51. Motion Budget｜动效预算

整个页面应存在“动效预算”。

一个场景中如果已有：

```text
琴键动画
+
进度动画
```

就应减少其他装饰动画。

不要出现：

```text
背景动画
+
卡片漂浮
+
按钮呼吸
+
粒子
+
琴键动画
+
进度动画
+
奖励动画
```

同时运行。

---

# 52. Transition Ownership｜动效归属

每个动画只能有一个主要负责的状态来源。

例如：

```text
琴键按下动画 → Pointer State
进度变化 → Practice State
解锁动画 → Reward State
```

禁止多个组件独立判断同一事件并分别播放动画，否则容易形成节奏混乱和重复反馈。

---

# 53. Layout Stability｜布局稳定性

练习过程中禁止因为状态变化造成布局跳动。

例如：

```text
准确率出现
按钮变长
歌曲标题换行
皮肤名称出现
```

不能导致琴键整体上下移动。

核心操作区域必须保持稳定。

推荐为可能变化的内容预留空间。

---

# 54. No Horizontal Drag｜禁止意外横向拖拽

iPad 与手机横屏使用中，横向拖拽尤其容易发生在：

```text
键盘
歌曲列表
装扮列表
弹窗
```

原则：

> 能固定的固定，必须滚动的区域才滚动。

禁止整个页面因为一个局部宽度计算错误出现横向滚动条。

---

# 55. Empty Space Is Content｜留白是内容

页面中的空白不是浪费空间。

留白承担：

```text
层级
呼吸
聚焦
节奏
专业感
```

不要用装饰填满留白。

尤其在桌面和 iPad 横屏上，应允许琴键与周围内容之间存在明显的呼吸空间。

---

# 56. Responsive Priority｜响应式优先级

在任何屏幕尺寸下，删除顺序固定：

```text
装饰
→
次要说明
→
次要数据
→
次要入口
```

绝不能优先压缩：

```text
琴键尺寸
当前任务
核心按钮
触控区域
```

---

# 57. Parent Trust｜家长信任设计

虽然家长不是主要操作者，但产品必须通过界面本身传递信任。

不依赖大段“安全说明”表达可信度，而依赖：

```text
没有广告
没有购买入口
没有注册门槛
界面干净
数据说明透明
设置简单
```

可信度来自整个产品的行为，而不是一块“我们很安全”的宣传区域。

---

# 58. Content Density｜信息密度控制

儿童核心操作页必须保持低信息密度。

成人设置与管理页面可以略高，但仍应避免后台式密度。

推荐：

```text
练习页：低密度
选歌页：低至中密度
成长页：中密度
设置页：中密度
```

不要让所有页面保持相同信息密度。

---

# 59. Design Consistency｜一致性不是复制

一致性并不意味着每个页面都长得一样。

应该保持一致的是：

```text
字体
颜色逻辑
间距体系
控件行为
声音语言
状态语言
导航逻辑
```

允许变化的是：

```text
内容
布局重点
音乐氛围
皮肤材质
```

这样才能形成系统而不是模板。

---

# 60. Progressive Enhancement｜渐进式增强

核心功能必须在最低复杂度下成立：

```text
触摸琴键
→
发声
→
视觉反馈
```

再逐步增加：

```text
跟弹
→
评分
→
成长
→
装扮
→
冒险
```

高级动画、背景氛围、非核心视觉效果全部属于增强层。

如果增强层失败，核心钢琴体验仍应正常。

---

# 61. AI Implementation Guardrails｜AI 开发防失控

任何 AI 编程模型在生成 UI 时，必须遵守以下硬性判断：

## 61.1 禁止“自作主张美化”

不要在没有需求的情况下：

```text
增加渐变
增加圆角
增加阴影
增加插画
增加动画
增加图标
增加卡片
增加 Badge
```

## 61.2 禁止“自动儿童化”

看到“儿童”二字，不得自动生成：

```text
糖果色
气泡
卡通
Emoji
贴纸
大头角色
大量彩色元素
```

## 61.3 禁止“自动游戏化”

看到“奖励”“成长”，不得自动生成：

```text
宝箱
金币
抽卡
排行榜
连击爆炸
任务墙
```

## 61.4 禁止“自动 Dashboard 化”

看到数据，不得自动创建：

```text
统计卡片
折线图
柱状图
数据墙
```

除非这些数据本身就是用户任务的一部分。

---

# 62. Five-Second Test｜五秒测试

任何新页面必须通过五秒测试：

5 秒内，用户应该能够回答：

```text
我在哪里？
我现在要做什么？
我下一步是什么？
```

如果无法回答，继续简化。

---

# 63. First Touch Test｜第一次触摸测试

第一次使用时必须验证：

```text
能否直接触碰琴键？
能否立即发声？
能否看见按下反馈？
是否存在误触？
是否需要阅读？
```

只要“必须先阅读”成立，就重新审查设计。

---

# 64. Three-Session Test｜三次使用测试

产品不能只追求第一次惊艳。

应该观察：

### 第一次

是否容易理解？

### 第二次

是否记得怎么操作？

### 第三次

是否开始形成自己的练习路径？

如果每次使用都必须重新学习界面，说明设计没有形成稳定语言。

---

# 65. 10-Minute Test｜十分钟练习测试

让儿童连续练习 10 分钟，重点观察：

```text
注意力是否被 UI 打断
手指是否容易误触
声音是否令人疲劳
提示是否让人焦虑
动画是否变得烦人
按钮是否经常被碰错
```

真正好的儿童界面，不只是第一眼漂亮，而是在第十分钟仍然安静。

---

# 66. Visual QA｜最终视觉验收升级版

## 66.1 第一层：结构

检查：

```text
主任务是否唯一
主视觉是否明确
空间是否有呼吸
```

## 66.2 第二层：细节

检查：

```text
文字 baseline
边界
圆角
图标光学对齐
键位比例
```

## 66.3 第三层：行为

检查：

```text
按下
拖动
松开
取消
切换
返回
```

## 66.4 第四层：时间

观察：

```text
声音
视觉
动画
状态
```

四者是否在正确时间发生。

## 66.5 第五层：删除

最后问：

> 如果删掉 20% 的元素，体验是否更清晰？

如果答案是“是”，继续删除。

---

# 67. Definition of Done｜设计完成的定义

一个功能只有同时满足以下条件才能称为设计完成：

```text
功能可用
用户可理解
触控自然
声音即时
反馈明确
页面稳定
视觉克制
无 Emoji
无 AI 模板感
无无意义动画
不破坏既有系统
```

“代码运行了”不等于“设计完成了”。

---

# 68. Final Product Character｜最终产品气质

最终产品应该让三类人产生三种不同但一致的感受。

### 儿童

> 我可以直接玩。

### 家长

> 这个东西很干净、很安全、很专业。

### 设计师 / 开发者

> 这里每一个细节都不是随便放进去的。

这三种感受共同构成产品的品牌质感。

---

# 69. Final Constitution｜最终设计宪法

本项目最终坚持以下九条：

```text
Piano First
钢琴永远第一

Touch Before Text
先触摸，后阅读

One Decision
一次只解决一个决定

Sound Is UI
声音就是界面

Quiet by Default
安静是默认状态

Growth, Not Commerce
成长，不是消费

Material Over Decoration
材质优于装饰

Meaning Before Motion
先有意义，再有动效

Delete Before Add
先删，再加
```

最终目标不是打造一个“功能很多的儿童 App”。

而是：

> **把复杂的儿童音乐系统，设计成一件孩子无需学习说明就敢于触碰、愿意反复使用、能够持续成长的数字乐器。**

---

# Appendix C — AI Preflight Checklist

每一次 AI 修改 UI 前必须先检查：

```text
[ ] 我是否理解当前页面的核心任务？
[ ] 我是否真的需要新增元素？
[ ] 我是否使用了已有 Token？
[ ] 我是否改变了既有信息层级？
[ ] 我是否引入了新的视觉语言？
[ ] 我是否增加了动画？
[ ] 我是否增加了 Emoji 或彩色 Unicode 图标？
[ ] 我是否把一个简单页面做成了卡片墙？
[ ] 我是否为了“儿童感”添加了卡通元素？
[ ] 我是否为了“高级感”添加了渐变或发光？
```

任何一项不合理，都应该停止并重新设计。

---

# Appendix D — One-Minute Design Review

当开发时间有限时，至少检查这十项：

```text
1. 第一眼能看见钢琴吗？
2. 第一触摸能听见声音吗？
3. 当前任务清楚吗？
4. 下一步清楚吗？
5. 页面是否太满？
6. 是否有多余动画？
7. 是否有多余卡片？
8. 是否用了 Emoji？
9. 是否出现 AI 模板感？
10. 删除一点后是否更好？
```

---

# Appendix E — Absolute Rule

> **不要让用户看到设计师有多努力。**
>
> **让用户只感觉到产品很好用。**

