# AGENTS.md — 开发约定

本文件是**任何人在（或任何 AI 在）修改本仓库之前必须首先阅读**的约定文件。

---

## 0. 两条强制前置阅读规则

### 规则 1（视觉类改动）：任何与前端视觉相关的部分，必须先读设计规范

> **《`儿童钢琴启蒙网页应用_设计规范_V3.0.md`》必须先读，然后才能动手。**
> 读法见 §14：按节读，并在回复里**声明命中了哪几节**；
> **引入新视觉语言 / 新增页面时必须通读相关章节**，纯数值与禁止项交给守卫测试。

适用范围（只要碰到其中之一就算「视觉相关」）：

```text
CSS / Design Token / 颜色 / 字体 / 字号 / 字重
圆角 / 边框 / 阴影 / 间距 / 版式 / 响应式断点
页面布局 / 组件结构 / 信息层级 / 文案措辞
图标 / 装饰 / 皮肤 / 主题 / 动效 / 过渡 / 空状态 / Loading
新增页面 · 新增组件 · 调整既有页面外观
```

**不允许**「先写出来再对照规范」，也不允许「凭印象认为符合」。规范有 2757 行、167 节，
其中包含硬性禁止项（Emoji、紫蓝渐变、玻璃拟态…）和精确数值（色板、圆角、阴影、动效时长），
这些无法靠记忆复现。

### 规则 2（其他改动）：必须先读开发规划

> **《`儿童钢琴启蒙网页应用_开发规划_V3.0.md`》**——尤其是第二十三章《给 AI 编程模型的最高优先级开发规则》。

音频、数据、状态机、评分、成长、皮肤解锁逻辑等改动，都要先确认没有违反那 18 条。

---

## 1. 精确路径

| 文档 | 路径 | 读它的时机 |
| --- | --- | --- |
| **会话入口（先读这个）** | `docs/AI-CONTEXT.md` | **进入仓库时（≤180 行：当前状态 / 不变量 / 文件地图 / 已定决策 / 下一步）** |
| 设计规范（设计宪法） | `儿童钢琴启蒙网页应用_设计规范_V3.0.md` | **任何视觉相关改动之前**（按节读，见 §14） |
| 开发规划 | `儿童钢琴启蒙网页应用_开发规划_V3.0.md` | 任何功能 / 架构改动之前（尤其第二十三章 18 条） |
| 进度与验收 | `docs/开发进度与验收.md` | 开始一项新工作之前（**检索**是否已有结论，不要通读） |
| 本文件 | `AGENTS.md` | 进入仓库时（**每轮自动注入**，故只放硬规则） |

---

## 2. 视觉改动的执行流程

```text
1. 读设计规范（规则 1）
2. 定位这个改动属于规范里的哪一节，按其规定执行
3. 只使用 src/styles/tokens.css 里已有的语义 Token，不在组件里硬编码数值
4. 走一遍 Appendix C 的 AI Preflight Checklist
5. 走一遍 Appendix D 的 One-Minute Design Review（十项）
6. 跑 npm run typecheck && npm run test
```

### Appendix C — AI Preflight Checklist（每次改 UI 前逐条确认）

```text
[ ] 我是否理解当前页面的核心任务？
[ ] 我是否真的需要新增元素？
[ ] 我是否使用了已有 Token？
[ ] 我是否改变了既有信息层级？
[ ] 我是否引入了新的视觉语言？
[ ] 我是否增加了动画？
[ ] 我是否增加了 Emoji 或彩色 Unicode 图标？
[ ] 我是否把一个简单页面做成了卡片墙？
[ ] 我是否为了「儿童感」添加了卡通元素？
[ ] 我是否为了「高级感」添加了渐变或发光？
```

### Appendix D — One-Minute Design Review（十项）

```text
1. 第一眼能看见钢琴吗？
2. 第一触摸能听见声音吗？
3. 当前任务清楚吗？
4. 下一步清楚吗？
5. 页面是否太满？
6. 是否有多余动画？
7. 是否有多余卡片？
8. 是否用了 Emoji？
9. 是否出现 AI 模板感？
10. 删除一点后是否更好？
```

---

## 3. 硬性禁止（违反即为缺陷，不是风格分歧）

以下均出自设计规范，**不得以「更好看」「更儿童化」「更高级」为理由绕过**：

| 禁止项 | 规范出处 |
| --- | --- |
| **任何 UI 使用 Emoji**（按钮/卡片/状态/奖励/皮肤/导航/空状态/数据字段） | §3.2 <br> §11.3（皮肤装饰） |
| 紫蓝渐变、默认彩虹渐变、玻璃拟态、霓虹发光边框、大量粒子、无限漂浮、无意义旋转、发光圆环 | §3.3 |
| 全部模块卡片化、所有组件都带阴影、游戏商城式 Badge 墙、无意义数据 Dashboard | §3.3 |
| 用「儿童化」制造儿童产品（卡通乐园、贴纸、糖果 UI、泡泡字、动物头像墙） | §3.4 |
| 大面积黑色阴影、彩色发光阴影、高斯模糊彩色光晕、卡片叠加多个 shadow | §7.3 |
| 大圆角（>20px）、所有元素都用 999px 胶囊 | §7.1 |
| 页面出现大量 700 / 800 / 900 粗体 | §6.4 |
| 用 10px / 11px 等极小字体传达重要信息 | §6.3 |
| 组件里大量硬编码颜色 / 圆角 / 阴影，而不是用 Token | §33 |
| 皮肤影响核心按钮尺寸、栅格、文字层级、交互逻辑、可访问性 | §11.1 |
| 自行设计新视觉体系、自行增加 UI、自行加入渐变发光 | §29、§61 |
| 购买 / 价格 / 充值 / 折扣 / 抽卡 / 随机开箱 / 支付 | §0.6、§48 |

**需要图形时只能用**：SVG / CSS Shape / 自绘 Icon / 受控图片资源。

---

## 4. 架构约束（开发规划第二十三章的落地）

| 规则 | 本仓库的落地方式 |
| --- | --- |
| 组件不直接改 localStorage | 只有 `src/core/progress/storage.ts` 读写，且只被 `ProgressStore` 调用 |
| 所有成长数据经 ProgressStore | `recordPracticeResult()` 是唯一业务入口（纯函数 + 单测） |
| React 不持有 AudioNode | 组件只调 `audioEngine.noteOn/noteOff/...` |
| 键盘不负责评分 | `PianoKeyboard` 只报告「哪个音按下 / 松开」 |
| 皮肤只能通过 Progress 事件解锁 | `skinResolver` 只读 progress；挑战模块完全不碰成长数据 |
| 皮肤只覆盖语义 CSS 变量 | `themeToCssVars()` 只输出 `--skin-*`，有单测校验不许越界 |
| 非核心模块不阻塞首次弹琴 | 装扮 / 小挑战 / 设置 / 自检全部 `React.lazy` 分包 |
| 所有 pointer 行为需真机验证 | 自检页 `#/tuning`；验收清单见 `docs/开发进度与验收.md` |

---

## 5. 常用命令

```bash
npm run dev        # 开发服务器
npm run dev:reset  # 开发服务器（清空 Vite 转换缓存后启动）
npm run test       # 566 个用例（冒烟 / 音色回归 / 音色预设 / 混音余量 / 设计规范守卫 / CSS 语法守卫 / 排版层级守卫 / 小屏适配守卫 / 皮肤对比度 / 琴键物理尺寸 / 五线谱几何·出题分档·对比度 / 按住提示与采样 / 挑战页布局 / 长按规则 / 文档完整性 / 录音回放 / 滑奏 / 曲库适配与简谱往返 / 节拍判定 / 节拍器咔声 / 内容包校验与存储 / 键盘层叠）
npm run typecheck  # 严格模式，必须 0 error
npm run build      # 生产构建
npm run icons      # 重新生成 PWA 图标
npm run shot       # 真机尺寸截图 + 几何读数（需先 npm run dev，见 §10）
```

改完必须全绿才算完成。

### ⚠️ 开发服务器缓存陷阱（踩过一次）

**症状**：浏览器报 `ReferenceError: X is not defined`，但源码里根本没有这个引用；
刷新、硬刷新都无效。

**原因**：Vite dev 会缓存模块转换结果，并且**可能一直不失效**。
最典型的触发方式：把一个改名操作拆成**两次编辑**（先删旧常量、再改引用），
中间那一瞬间的坏代码被缓存下来，之后浏览器拿到的就是那份坏掉的转换。

**预防**：**一次编辑里完成改名**，不要拆成「先删定义、再改引用」两步。

**同理，新增引用也要一次编辑完成。** 这条第二次踩到：
给 `PracticePage` 加录音回放时，先加 `recording={recording}`、下一笔才加 `import { buildRecording }`，
中间那份「用了没导入」的版本被缓存下来 —— 之后浏览器一直报
`ReferenceError: buildRecording is not defined`，而 `npm run typecheck` 全绿、源码也是对的。

**判断方法**（决定性）：直接看服务器下发的内容，而不是看源码 ——

```powershell
# 换成本地端口与真实路径即可；比较「服务端下发的」与「源码里的」
$wc = New-Object System.Net.WebClient
$wc.Encoding = [System.Text.Encoding]::UTF8
$wc.DownloadString('http://localhost:5173/src/features/practice/PracticePage.tsx') -split "`n" |
  Select-String 'buildRecording'
```

如果下发的模块缺少源码里明明有的那一行，就是缓存问题。

**解决**：结束占用 5173 的进程，然后 `npm run dev:reset`（即 `vite --force`）。

**这次是 CSS 版本（第三次）**：改了 `piano.css` 的 `bottom`，源码正确、也没有别的规则覆盖，
但实测位置**仍然是旧值** —— Vite 把 CSS 转换也缓存了。
**判断方法**：让探针把 `getComputedStyle` 的值打印出来（`key-stacking.js` 会打印提示点的 `bottom`），
一眼就能区分「CSS 没生效」和「我自己算错了」。**解决**：同样 `--force` 重启。

**顺序也很重要**：改完**先跑 `npm run typecheck`，再看页面**。
有一次把「漏改一处旧变量名」当成了缓存陷阱，而 `tsc` 一眼就指出了行号 —— 白跑了一轮浏览器探针。


> 另外：`npm run shot` 现在能一眼看出这类问题 —— 白屏兜底会把
> `ReferenceError: xxx is not defined` + 调用栈打印出来（`--script scripts/flows/dump-error.js`）。

---

## 6. 当前符合性状态（务必先看）

⚠️ **现有代码的视觉层大部分写于设计规范纳入开发框架之前。**

整改进度（详见 `docs/开发进度与验收.md` 的《设计与规范符合性审计》）：

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| 批次 1 | Token 与数值（色板 / 圆角 / 阴影 / 字重 / 字号 / 字体栈 / Easing / Token 命名 / 胶囊） | ✅ 已完成，现已符合 §4.2 §5.1 §6.1 §6.3 §6.4 §7.1 §7.2 §7.3 §13.3 §25 §33 |
| 批次 2 | 去 Emoji：新建 `src/shared/ui/Icon.tsx`，全部改用自绘线性 SVG 图标 | ✅ 已完成，现已符合 §3.2 §8.1 §8.2 §11.3 §12.2；有自动化守卫测试 |
| 批次 3 | 皮肤材质化：默认皮肤「原木」、8 套材质方向重做、引导色拆白/黑两个 | ✅ 已完成，现已符合 §3.3 §10.2 §10.5 §11.2 §11.4 §46 §47；有对比度约束测试 |
| 批次 4 | 装扮页改为「琴键样品板」：`SkinSample` 直接渲染真实琴键材质，删除 `mark` 徽章字段与 5 个死图标 | ✅ 已完成，现已符合 §0.9 §12.1 §12.2 §53 §54 |

**审计发现的偏差已全部整改完毕。** 因此：

- ✅ **`src/styles/tokens.css`、`src/shared/ui/Icon.tsx`、`src/features/skins/*` 均已符合规范**，
  可以直接作为依据。改皮肤配色会被 `skins.contrast.test.ts` 的 WCAG 断言拦住。
- ✅ **新增任何图形必须用 `<Icon name="..." />`**，不要内联 `<svg>`、更不要用 Emoji 或字符号
  （`src/test/design-guard.test.ts` 会在测试阶段拦住）。
- ✅ **表达「皮肤」用 `<SkinSample />`（真实琴键材质），不要用图标或色卡代替。**

新写的视觉代码必须符合设计规范；遇到与既有代码冲突时，**以规范为准，不要以既有代码为准**。

---

## 7. 琴键尺寸是物理尺寸，不是比例

`src/features/piano/keySize.ts` 按**真实钢琴白键 23.5mm** 反推 CSS px（浏览器不提供 PPI，
只能用「已知机型表 + devicePixelRatio」估算）。

- **不要**把琴键改回「铺满容器」—— 那会让手指跨度与真实钢琴脱节，破坏需求本身
- **不要**把琴键放大到超过真实尺寸
- 新机型不在 `DEVICE_PPI` 表里时会退回兜底值并标记为「估算」，界面会带「约」字；
  发现估算不准就把机型补进表里
- **产品只提供 15 键**（C3–C5）：两个八度白键 + 10 个黑键，
  **中央C 是第 8 个白键 = 正中间**。钢琴课第一步教的就是「先找中央C」；
  8 键只有一个八度、中央C 只能落在**最左边**，那会让这个最重要的空间认知从一开始就是错的
  （这条覆盖了「8 键」与更早的「初学者不出现黑键」两次设定）
- **键宽永远是真实 23.5mm —— 不再压缩**：键盘允许**宽出屏幕**，由横向拖动来选取区域
  （`resolveWhiteKeyWidth({ allowOverflow: true })`）。这解开了先前的死结：
  小屏上「键宽真实」与「中央C 能在中间」不可能同时满足，允许溢出之后两者兼得
- **默认视窗以中央C 为中心**（`PianoKeyboard` 把 C4 滚到视窗中间）：
  钢琴课第一步教的就是「先找中央C」，它不该落在边上
- **键盘高度按真实长宽比反推**：`高 = 键宽 × 6.38`（真实白键 150 : 23.5），再用可用高度封顶。
  屏幕高度永远小于一台真琴，所以绝对长度达不到，但长宽比能做到 —— 否则琴键会变成细长条
- **装键盘的容器必须写 `min-width: 0`**：flex / grid 子项的 `min-width` 默认是 `auto`，
  它不肯收缩到比内容更窄，于是被宽出屏幕的键盘撑开、把**整个页面**拉宽
  （实测 `main.free__stage` 被撑到 1832px、页面溢出 1038px）。横向滚动必须发生在
  `.pk-frame` 里，不是页面上
- **键的材质要有实体感**（产品反馈过「阴影与间距太细太窄」）：白键缝隙 ≥2px（真琴约 1mm≈3px，
  不是发丝线）、前端有 ≥6px 的「前脸」、黑键的接触阴影比白键更厚（它高出一层）。
  阴影**只用 `--skin-key-shadow`**，单层、中性色，不硬编码、不彩色发光（§7.3 / §33）
- 加粗缝隙**不会改变音距**：`box-sizing` 是 border-box，边框算在 122px 里 ——
  变窄的只是「可见键面」（121→119px），这正是真琴的样子
- **黑键必须画出来**：孩子靠「两个黑键左边那个白键就是 C」来找中央C
- 黑键**是叠在白键上的**：加黑键不会让白键变窄，物理尺寸不变；黑键可正常按响，
  所以练习页上按错黑键 = 弹了一个错音（与真实钢琴一致）
- 8 / 25 键的布局代码保留备查，但**不出现在界面 / 存档 / 曲库数据里**
  （改档位表只改 `KEYBOARD_SIZES`，界面会自动跟着变）
- **黑白键的层叠由「层」决定，不由键决定**：`.pk__whites`(z-index 1) / `.pk__blacks`(z-index 2)。
  没有层的 z-index 时，白键上的引导样式（`.pk-key.is-guide` 的 z-index 3）会翻到黑键上面 ——
  黑键被白键键体切掉一半，而且**按不下去**（命中被白键抢走）。`keyStacking.test.ts` 守着这条
- 白键上的「手指提示点」必须落在黑键带（顶部 62%）**下方**，否则会被黑键挡掉一半
- 键盘只画出来还不够 —— **真机上有没有渲染出来**由 `#/tuning` 的「键盘档位 / 结构」一行
  （白键数 / 黑键数，直接数 DOM）回答：白 15 / 黑 0 = 拿到的是旧页面；白 15 / 黑 10 = 渲染了
- 相关验收：拿尺子量屏幕白键宽度，确认与 `#/tuning` 的读数一致

---

## 8. 长按属于应用，不属于系统

**整个应用都不允许系统自带的「长按选取文字 / 放大镜 / 呼出菜单」。**

落地位置：`src/styles/base.css` 里 `html, body, #root` 的规则。
`user-select` 是**继承**属性，所以必须写在文档根节点上。

### 为什么这是硬性规则，而不是样式偏好

这个应用的主要使用方式就是**按住琴键不放**。只要长按能被系统抢走：

1. iOS Safari 进入文字选择模式，弹出放大镜 —— 孩子看到的是一个跟自己无关的系统 UI
2. **后续 pointer 事件被系统吃掉**，表现是「按着按着音就哑了」；
   由于 `pointercancel` 也可能被吞掉，琴键还会停在按下态

所以这不只是「不许选文字」的外观问题，而是**交互被系统中断**的功能问题。

### 改样式时注意

- **不要**再写 `user-select: text / auto / all`，会被 `browser-compat.test.ts` 拦住
- 极少数确实需要复制文字的地方（例如让家长复制自检页读数）加 `.is-selectable`
- 琴键自己的 `-webkit-touch-callout: none` / `touch-action: none` 继续保留 ——
  根节点管「选择」，`.pk` 额外管「滚动与缩放」，两者职责不同
- `PianoKeyboard` 上的 `onContextMenu` 屏蔽继续保留（桌面右键 / iPad 长按菜单）

> 这条**只在真机上才看得出来**，jsdom 永远不会复现，所以只能靠守卫测试 + 人工验收。

---

## 9. 键盘永远是「拿走剩余空间」的那个元素

`.chl__stage`（两个小挑战页共用）用 **flex 列**，不用 `grid-template-rows` 按序号分行。

### 原因：踩过一次

舞台里的谱面（`.staff`）与「刚才的音」都是**条件渲染**的。
而 `grid-template-rows` 是按子元素**顺序**分配行的：

```text
听音找键：prompt / keyboard / hint
          → 键盘落在第 2 行 = 1fr  ✅ 高度正常

看谱找键：prompt / staff / keyboard / hint
          → 键盘被挤到第 3 行 = auto ❌
```

`auto` 行的高度是**不确定**的，于是 `.pk-frame { height: 100% }` 无法解析，
琴键退回 `.pk` 的兜底 `min-height: 150px` —— 在 iPad 上就是「琴键变得很矮」，
而同一份代码在听音找键里完全正常。**故障只出现在多了一个条件子元素的那一页。**

flex 里剩余空间由 `flex-grow` 明确指定，与子元素个数无关，所以不会再出现这种连带故障。

### 写新页面时

- 谁拿剩余空间要**显式写 `flex-grow`**，不要依赖 DOM 顺序
- 键盘用 `flex: 1 1 150px`，基线 150px 与 `.pk` 的 `min-height` 一致
- 让某个元素可压缩时要同时写 `min-height`（显式值，不是 0），
  否则 flex 子项默认的 `min-height: auto` 会让它根本压不下去
- 但**光有下限不够，还要有上限**：谱面按比例在横屏 iPad 上会长到 276px，
  把琴键压得很矮。所以 `.chl__stage > .staff` 还有 `max-height: 34%`
  （键盘是**作答**的地方，给它大头）
- 谱面**横向铺满**（`width: 100%`）：它是横的，铺满宽度时音符与标签一起变大
- 不变量由 `src/features/challenges/challengesLayout.test.ts` 守住

### 谱面不套卡片

`<StaffNote />` 直接铺在舞台底色上，**没有**卡片外壳（原来的 `.staff-panel`
已删除）。卡片会给出一块与内容无关的填充色块，而谱面本来就是「纸上的谱」，
自己就能立住（§3.3 不要把每个模块都做成卡片）。

代价是**谱面要自己在舞台底色上撑住可读性** —— 所以：

- 谱线、线音名、简谱方框用 `--color-text-tertiary-strong`（约 3.0–3.5:1，满足
  WCAG 1.4.11 的 3:1）。**不要退回** `--color-border-strong`（1.2–1.5:1，太淡）
  或 `--color-text-tertiary`（2.2–2.6:1）
- 文字类的标注（简谱数字、唱名拼音）要 4.5:1；`--color-accent` 只有 3.4–4.0:1，
  所以文字用 `--color-accent-strong`
- 这些都由 `src/features/notation/staff.contrast.test.ts` 算出来守住 ——
  它从 staff.css 读实际用的 token 名，再去 tokens.css 解析颜色值，改了 CSS 就会失败

---

## 10. 视觉问题先用工具量，不要靠读 CSS 猜

**布局问题的第一反应是 `npm run shot`，不是读 CSS。**

```bash
npm run dev                                            # 先起开发服务器
npm run shot -- "#/challenges/staff" --click 开始       # iPad 竖屏 810×1080
npm run shot -- "#/challenges/staff" --click 开始 --width 1080 --height 810
```

它用 Chrome DevTools Protocol 在**真实布局引擎**里跑，产出放进 `.shots/`：

- `<name>.png` —— 按 iPad 视口渲染的截图
- `<name>.json` —— 关键元素的真实几何 + **命中测试** + **相交元素列表**

需要**连续操作**（点几步、模拟按键、等元素出现）时用 `--script` 传一个 JS 文件；
里面 `return` 的对象会被原样打印。现成的例子：

```bash
npm run shot -- "#/practice/mary-lamb" --script scripts/flows/result-overlay.js
npm run shot -- "#/任何路由" --script scripts/flows/dump-error.js   # 白屏时读兜底错误
```

两个已经踩到的探针限制：

- 页面里的 `el.click()` **不算用户手势**，`AudioContext` 会停在 suspended。
  所以启动参数里带了 `--autoplay-policy=no-user-gesture-required`。
  真机上那次点击是真手势，这不是应用的问题
- 挂载检查失败只**警告**不中断 —— 挂不上的时候恰恰最需要把脚本跑起来看现场

`src/features/practice/practiceReducer` 这类纯逻辑有单测，
但**布局和触控在 jsdom 里永远是「看起来正常」的**：没有布局引擎，
`getBoundingClientRect()` 恒为 0。这个项目已经因此吃了三次亏：

| 故障 | 为什么 jsdom 测不出来 |
| --- | --- |
| 看谱找键的琴键很矮 | grid 行归属由子元素个数决定，没有布局就没有行高 |
| 长按被系统抢走 | 系统行为，jsdom 没有系统 |
| 拼音被遮挡（字形盒超出 viewBox） | 没有布局就没有字形盒 |

`.shots/` 已 gitignore。**能读到几何就不要凭肉眼**：
`overlapping` 会直接告诉你「被谁挡住了」，比截图比对可靠得多。

> 这个模型看不到图片时也没关系 —— `getBBox()` / `elementFromPoint` 给的是**数字**，
> 而数字比一眼观感更适合下结论。

---

## 11. 绝不用 shell 改这个仓库里的 UTF-8 文件

**这条是用一次真实事故换来的：`docs/开发进度与验收.md` 被一条 PowerShell 命令写坏了
1381 个字符（462 / 793 行受影响），而且这个仓库不是 git 仓库，没有第二份副本。**

### 事故经过

一条看起来无害的「批量替换」命令：

```powershell
# ❌ 永远不要这样改这个仓库里的文件
(Get-Content $f -Raw) -replace '旧','新' | Set-Content $f -NoNewline
```

跑完之后文件里出现了 1381 处 `\uFFFD?`，中文标点、Emoji、行尾字符成片丢失。
**`Get-Content` / `Set-Content` 的编码推断在中文内容上不可靠。**

### 规则

- 改文件**只用 `edit` / `write` 工具**（它们明确按 UTF-8 读写）
- 批量/正则替换同理：要么用 `edit` 的 `replace_all`，要么用 **Node 脚本**
  （`fs.readFileSync(p,'utf8')` → 处理 → `fs.writeFileSync(p, s)`），不要走 PowerShell 的管道
- 需要 `.NET` 时也必须显式编码：`[System.IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)`
- 大改之前先备份：`Copy-Item $f "$f.bak"`（`.bak` 已在 gitignore 之外，用完记得删）

### 真出事了怎么办

这个仓库没有 git，所以只有一条路：**从 DSH 的会话记录里重放**。
会话记录 `~/.dsh/sessions/<工作区>/<会话>/session.jsonl.zstd` 是追加写的 JSONL，
里面有每一次 `write` 的完整 `content` 与每一次 `edit` 的 `old_string`/`new_string`，
按顺序重放就能还原。

```bash
node scripts/decode-zstd-frames.mjs <session.jsonl.zstd> session.jsonl   # 多帧 zstd，Node 只解第一帧
node scripts/replay-file-history.mjs session.jsonl "文件名片段" restored.md
node scripts/verify-restored-doc.mjs                                     # 校验：未损坏的行必须逐行对上
```

**校验方法很重要**：损坏只替换字符、不删行，所以「损坏文件里没有损坏标记的行」
应当能在恢复结果里逐行原样找到 —— 331 / 331 全中才算恢复成功。

---

## 12. 音色是数据，不是代码

**想改音色 / 加音色，只改 `src/core/audio/voicePresets.ts`，不要动 `PianoSynth.ts`。**

`voicePresets.ts` 里每套音色声明自己的频谱滚降、力度倾斜、余音时长、制音器快慢、
拍频、琴体共振峰、颤音、击弦噪声；`PianoSynth.ts` 只是「照着预设把音造出来」，
自己不含任何音色参数。

### 改音色时必须守住的一条

**力度必须改变频谱倾斜的「形状」，不能只给所有高次分音乘一个统一倍率。**

统一倍率 = 只有音量在变，那是「电子琴感」最主要的来源（这个缺陷真实存在过）。
正确写法是 `(k/3)^指数`，指数随力度从负到正 —— 支点取 k=3，
因为钢琴前两个分音几乎不随力度变化，变化发生在更高的分音上。
`voicePresets.test.ts` 有一条测试专门拦这个：它断言**高次分音随力度变化必须大于低次分音**。

### 两条容易踩的

- `PianoVoice` 的**门包络必须是第一个创建的 GainNode** ——
  音色回归测试用 `ctx.gains[0]` 取它，把新节点建在前面会让 5 条测试一起失败
- 加颤音会多一个振荡器；`PianoSynth.test.ts` 断言单音的分音数量上限，
  所以颤音只加在需要的预设上（目前只有电钢琴）
- 音色是**存档数据**（`AppSettings.voicePreset`）：改 id 要同时改 `migrations.ts`

### 音色 ≠ 皮肤

皮肤是外观材质（只覆盖 `--skin-*`，§11.1），音色是声音。两者互不影响，也**不做联动** ——
换皮肤不应该改变听到的东西。

---

## 13. 电平：一个音就能把满刻度用完

**`PianoVoice` 的所有分音是同相位起音的**，所以**单个音**的瞬时峰值就接近
分音振幅之和（约 0.95）—— 一个音几乎用满刻度，第二个音必然溢出。

所以链条上固定有两道防线（`src/core/audio/mixHeadroom.ts`）：

```text
voice → pianoBus(余量 0.35) → dry + reverb → limiter（减少饱和）→ master(音量) → 软削波 → 输出
```

- **余量在总线上**，不在单个音里：这样和弦才有的涨落空间
- **软削波必须在 `master` 之后**：它是「不管音量多大都不可能触顶」的唯一保证
- **不要用 `DynamicsCompressorNode` 当限幅器**：它不是砖墙限幅器。
  实测配成 threshold −10dB / ratio 6 / attack 3ms 之后，八音和弦的输出仍然到 **1.19**，
  起音瞬态直接在 attack 时间里穿过去了

### 改音色 / 加音色时

- 新预设**必须量一次响度**并填 `outputGain`，否则切过去会忽然变小或变大：

  ```bash
  npm run shot -- "#/" --script scripts/flows/preset-loudness.js     # 等响
  npm run shot -- "#/" --script scripts/flows/audio-clipping.js      # 削波（前后对比）
  ```

- 这两件事 jsdom 都测不了（没有 Web Audio），**只能离线渲染量**。
  纯数学部分（软削波曲线）由 `mixHeadroom.test.ts` 守住。

---

## 14. Token 预算（本文件每轮自动注入，所以这里只放规则）

完整方案、反例账本、以及为什么这样定 —— 在 `docs/AI-CONTEXT.md` §7，**按需读，不要每轮读**。

**开场读**：`AGENTS.md`（自动）+ `docs/AI-CONTEXT.md`。
三份大文档（规范 2758 行 / 规划 1585 行 / 进度 1490 行，合计 108 KB ≈ 7 万 token）**改为检索，不通读**。

**六条规则**：
① 大文档先 `grep` 定位，再 `read` 带 `offset/limit`（≤120 行）；同一次会话不重复读同一份；
② 同一文件的改动一笔完成，互相独立的调用放**同一条消息**；
③ 探针 / 测试的原始输出写文件，只回 ≤5 行结论；
④ 迭代只跑相关的那一个测试文件，收尾跑**一次**全量 + 构建；
⑤ **回复短**，细节写进文档 —— 对话里的每个字都会在之后的每一轮重复付费；
⑥ 不复述需求与规范，不重开已定决策。

**收尾自检**：这一轮的内容，如果之后 30 轮都要重复付费，我会怎么写？

---

## 15. 滑动是手势，不是 N 次按键

手指擦过琴键时，**同一段手速在窄键上会触发多几倍的音**（8 键 vs 25 键差 5 倍）。
每个音都带着制音器尾巴和一记明亮起音，叠起来就是「嘈杂」。

**所以滑动按滑奏处理**（`src/features/piano/useKeyboardInput.ts`）：
上一个键按住不到 `GLIDE_HOLD_MS`(150ms) 就换键 → 滑奏（逐键弹奏不受影响）；
滑奏的音按 `GLIDE_VELOCITY`(0.6) 发声、限速到 `GLIDE_MIN_GAP_MS`(65ms) 一个音；
被限速挡下的音变成「待补发」，手指停在该键上就补上 —— **不丢音**。

**不要再从电平上找这类问题的原因**：实测滑动输出峰值只有 0.90、软削波几乎没工作。
读数与三次排查记录在 `docs/开发进度与验收.md`《滑动琴键的嘈杂音》；
`useKeyboardInput.glide.test.ts` 守住策略。

---

## 16. 内容包 ≠ 内置曲库

用户导入的曲目（教材等）走 `src/core/content/`，**与 `features/songs/songData.ts` 是两套东西**：

- **应用不含任何教材内容**：教材的编配受著作权保护，而这个应用部署在公开地址 ——
  导入的内容只存在**用户自己设备的 localStorage** 里，不上传、不进构建产物
- **导入曲目的 id 必须带包前缀**（`packId:songId`，用 `packSongId()` 拼）：
  否则 `bestAccuracyBySong` 里「同一首曲子」的历史成绩会串到别的曲子上去
- **包是只读快照**：导入后不可变，要改就重新导入 —— 否则 id 漂移，已有成绩对不上
- **校验一律拒绝、不修补**（`packSchema.ts`）：曲谱错一个音就是在教错音，
  宁可让家长看到一条明确的错误，也不要「尽力而为地猜」
- 内容存储只有 `packStorage.ts` 能碰（与 `progress/storage.ts` 同一规矩），
  坏数据**隔离保存**而不是直接丢掉

---

## 17. 样式表也会被静默截断（改完 CSS 要看完构建输出）

**事故**：一个批量改样式的脚本把 `src/features/tuning/tuning.css` 结尾的
`.tune__keys, .tune__sizes { grid-column: 1 / -1; }` 削成了孤立的 `.tune__keys,`。

**它是静默的**：`{` 与 `}` 数量**仍然配平**，所以页面不报错、`typecheck` 0 error、
555 个用例全绿，只是宽屏下自检页的键盘不再横跨两列。
**唯一的信号**是 `npm run build` 输出里的一行 `▲ [WARNING] Unexpected "}" [css-syntax-error]`。

- **改完 CSS 逐行看完构建输出**，不要只看最后那行 `✓ built in`
- `src/test/css-syntax.test.ts` 守着这条：每个 CSS 都过一遍 esbuild，有警告即失败。
  它**必须跑在 `@vitest-environment node`**（jsdom 的 `TextEncoder` 在另一个 realm，
  esbuild 会报 `Invariant violation: ... instanceof Uint8Array`）
- 定位手法：`scripts/css-syntax-locate.mjs` 逐文件交给 esbuild，直接打印文件名 + 行号 + 上下文
- 它只抓**语法**：脚本把一整条规则删得干干净净时抓不到 ——
  所以规矩仍是 §11，批量改样式用 `edit` / Node 脚本，不要用带正则的 shell 管道
- 细节与完整复盘见 `docs/开发进度与验收.md`《样式表被批量脚本静默截断》

### 第二种截断签名：**注释夹在选择器列表中间**

第一次是靠「多余的 `}`」发现的；但同一次事故还有**不留语法错误**的一种：

```css
/* 速度档的分段控件 */
.pp__tempo,

.pp__tempo-btn,

.pp__tempo-btn.is-active,

/* ---------------- 舞台 ---------------- */
.pp__stage { display: grid; ... }      ← 上面三个被并进这条规则
```

声明块被删掉后只剩以逗号结尾的悬空列表，于是这些选择器**被合并进下一条规则**：
练习页的速度档因此变成**竖排 3 行**、还套上了舞台的 grid，顶栏被撑高 110px、键盘矮了 110px。
**语法完全合法**：esbuild 不报警、括号也配平、页面不报错、测试全绿 —— 只能靠人眼或这条检查发现。

- `src/test/css-syntax.test.ts` 第三条测试专门守这个签名：
  **注释之前还剩下真正的选择器文本 = 损坏**（先剥掉注释再判断，所以「文件头注释 + 区块注释 + 选择器」不会误报）
- 这个仓库从不把注释写进选择器列表；要写只写在规则**上方**
- 扫描工具：`node scripts/css-truncation-scan.mjs`（列全部可疑位置）
- 也别再用「按选择器删整条规则」的脚本删单个属性：`scripts/css-edit.mjs` 的 `removeRule`
  是按选择器匹配的，同一个选择器出现两次时会一起删（踩过：把 `.pp-result__title` 的正规规则删了）

---

## 18. 排版层级是一张表，不是手感

**字号只能来自 `tokens.css` 的 7 档，每一档绑定一个「角色」** ——
不是「这个标题大概 20px 吧」。层级由「它在页面任务里排第几」决定，
不由「它是个标题还是个按钮」决定：自检页的面板标题与设置页的分组标题都是区块标题，就必须一样大。

| 层级 | token | 字号 | 用在哪 |
| --- | --- | --- | --- |
| 一级 · 页面核心标题 | `--font-size-display` | 42–56 | 一个页面只有一个主角的地方（完成页标题、星星行） |
| 一级 · 页面标题 | `--font-size-h1` | 24–28 | 所有页面顶栏标题（含首页品牌名、练习页歌名） |
| 二级 · 主要状态数字 | `--font-size-metric` | 32–40 | 挑战连击 / 判定 / 装扮页成长数值 |
| 二级 · 区块标题 · 主操作 | `--font-size-h2` | 18–20 | 页内分节标题、大按钮 |
| 三级 · 正文 | `--font-size-body` | 16 | 说明、列表内容、普通按钮 |
| 三级 · 辅助文字 | `--font-size-label` | 14 | 副标题、元信息、统计、次要按钮 |
| 四级 · 最小说明 | `--font-size-micro` | 12 | 脚注、单位、提示（**下限，不再更小**） |

- **写不出属于哪一层，就说明这个元素还没有明确的层级** —— 先想清楚，再写样式
- 组件里出现 `font-size: 15px`、`font-weight: 600`、`line-height: 1.6` 会被
  `src/test/typography.test.ts` 拦住；新页面要**先在守卫的角色表里登记**
- **`var(--font-size-*)` 必须真实存在**：曾有过 `var(--font-size-title)` 这种从未定义的引用，
  整条声明静默作废、字号靠继承，页面不报错、测试也不失败 —— 守卫现在专门拦这条
- **行高只用 4 档**：`--line-height-none`（大号数字）/ `tight`（标题）/ `body`（正文）/ `relaxed`（多行说明）
- **顶栏全站 64px 高**：`.page__bar` / `.home__bar` / `.pp__top` / `.chl__top` / `.tune__top`。
  两条让 64px 真的成立的机制（都实测过）：
  栏内控件用 `--tap-target-compact`(44px)，否则 56px 的按钮把栏撑到 80px；
  栏自身内边距 `var(--space-2) var(--space-4)`，否则 12px 内边距 + 44px = 68px
- **正文列**：分区间距统一 `--space-4`；阅读型页面版心 `--content-max-width`(720px)，
  网格型页面（曲目 / 装扮 / 自检）用满可用宽度（它们靠 `auto-fill` 自适应）
- **删 CSS 规则之前先跑 `node scripts/dead-css.mjs`**：
  有一次为了删一条 30px 的覆盖规则，把 `.pp-result__title` 的正规规则一起删了 ——
  完成页的核心标题掉回继承字号，**是新加的守卫当场抓到的**（`.pp-result__title 是（无 font-size）`）
- 量排版用 `npm run shot -- "#/settings" --script scripts/flows/typography.js`
  （输出各层级实际字号、顶栏高度、版心、溢出与被裁文字）

---

## 19. 小屏不是「缩小版」，是按 §56 的顺序删与压

**小屏的两种故障形态**（都实测过，且都**只在小屏出现**）：

| 形态 | 实测 |
| --- | --- |
| **纵向放不下** | `.chl` 是三行 grid（顶栏 / 舞台 / 底栏），底栏是**核心按钮不能删**；667×375 时舞台只有 219px 而内容要 383px → 溢出 84px，提示行被推出屏幕，键盘掉到 150px 兜底 |
| **横向被撑宽** | `.pp__top-actions` 是 `flex: 0 0 auto`，把**整页最小宽度**顶到 540px → 320 视口下页面 556px，「听一遍 / 速度档 / 开始跟弹 / 严格一点 / 清空成长数据」全被挤出屏幕（**点不到**） |

**顺序照 §56 来**：装饰 → 次要说明 → 次要数据 → 次要入口；
**绝不优先压缩**琴键尺寸 / 当前任务 / 核心按钮 / 触控区域。落地时依次是：

1. **顶栏收高**（64 → 48，`@media (max-height: 560px)`）—— 栏内控件仍是 44px（§25）
2. **次级说明让位**（`.chl__sub { display: none }`、「刚才的音」行）
3. **行间距与固定高度**（`.chl__prompt` 的 96px 固定高放开、gap 12 → 8）
4. **谱面改成高度驱动**（`height: clamp(48px, 18vh, 116px); width: auto`）——
   原先 `width: 100%` + `max-height` 会把 3.8:1 的谱面压成一条细带，音符小到看不清；
   高度驱动时宽度按比例跟着走，小屏**反而更清楚**
5. **最后才动键盘的纵向长度**（`clamp(112px, 30vh, 150px)`）—— 触控宽度仍是真实 23.5mm、横向可拖动

三条容易漏的细节：

- **`.pk` 自带 `min-height: 150px`**：容器被收矮后它不肯缩，于是溢出容器、被 `overflow: hidden`
  裁掉一截（键的下缘与音名都被切）。小屏必须同时写 `.chl__keyboard .pk { min-height: 0 }`
- **只写 `flex-wrap: wrap` 不会换行**：`.pp__stats` 自己是 `flex: 0 0 auto`，会按最大内容宽
  （382px）定宽。必须同时给 `flex: 0 1 auto` + `min-width: 0`
- **分段控件那一行**（设置页「标签 + 分段控件」最小内容宽 313px > 320 视口的可用 288px）
  要把 `.settings__row` 改成可换行，否则档位按钮被挤出屏幕

**验收方式**（jsdom 看不见这些，只能靠真实布局引擎）：

```bash
npm run shot -- "#/challenges/staff" --click 开始 --script scripts/flows/challenge-layout.js --width 667 --height 375
npm run shot -- "#/practice/mary-lamb" --script scripts/flows/overflow-hunt.js --width 320 --height 568
```

`challenge-layout.js` 用 `elementFromPoint` 在谱面中心与四角做命中测试（直接问「谁在上面」），
`overflow-hunt.js` 列出把页面撑宽的元凶与被挤出屏幕的按钮。
矩阵至少覆盖：**320×568 / 375×667 / 667×375 / 844×390 / 810×1080 / 1080×810**。
关键声明由 `src/test/small-screen.test.ts` 守着（jsdom 里媒体查询不生效，只能守声明）。
