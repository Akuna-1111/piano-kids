# 儿童钢琴启蒙网页应用 — 产品与开发规划 V3.0

参考方向：V2EX《放假期间给娃做了个儿童钢琴网页版》

目标平台：iPad / 手机 / 桌面浏览器
主要使用姿态：横屏优先
产品形态：静态 Web App + PWA，零广告、零账号、零内购、打开即玩

---

## 0. V3.0 本次升级摘要

V2.0 已经完成了“钢琴 + 曲谱 + 游戏化 + 皮肤”的功能拼图，但模块之间存在一定的功能堆叠风险。V3.0 不再继续增加功能数量，而是建立一套明确的产品主线：

> **玩琴 → 学会 → 完成 → 获得反馈 → 解锁装扮 → 再玩琴**

核心原则调整为：

1. **练琴是主任务，皮肤是奖励层，不是主任务。**
2. **星星是成长积分，不是“消费货币”。达到阈值自动解锁，不会被消耗。**
3. **核心 UI 永远保持稳定，皮肤只改变钢琴区域和有限的氛围层。**
4. **音频、键盘输入、练习评分、成长数据分别解耦。**
5. **所有业务状态由单一数据源驱动，避免多个 Context 互相更新。**
6. **所有内容尽量静态化，本地运行即可，不依赖后端。**
7. **儿童体验优先于“功能丰富”，避免排行榜、稀有度炫耀、抽奖、倒计时压力。**

---

# 一、产品定位

## 1.1 产品一句话

一个打开网页就能直接弹的儿童钢琴启蒙工具，让孩子通过“触摸—听觉—视觉—完成感”自然认识音高与节奏。

## 1.2 用户

### 儿童

主要目标：

- 敢于按下第一个琴键
- 认识高低音和音名
- 能跟着提示完成简单旋律
- 愿意重复练习
- 因为完成而获得成就感

### 家长

主要目标：

- 打开即用
- 没有广告
- 没有支付诱导
- 不需要账号
- 数据尽量留在设备
- 能看到孩子长期练习成果

## 1.3 产品排序

优先级固定为：

> **声音 > 手感 > 引导 > 内容 > 成长 > 装扮 > 其他玩法**

任何新增功能都必须回答：

> 它是否让孩子更容易开始弹、继续弹或学会一个音乐概念？

不能回答“是”的功能不进入核心版本。

---

# 二、体验架构 V3

## 2.1 信息架构

建议从“功能菜单型”改成“任务入口型”。

### 首页

只保留 4 个一级入口：

- **跟我弹**：歌曲练习主入口
- **自由弹**：直接进入钢琴
- **小挑战**：节拍 / 音高 / 音色小游戏
- **我的装扮**：皮肤、星星、勋章

顶部只保留：

- 当前歌曲或模式
- 星星数量
- 设置

不要把“动物模式、打鼓模式、冒险模式、皮肤商店、录音”等全部平铺成一级入口。

## 2.2 核心练习闭环

```text
选择歌曲
  ↓
先听一遍
  ↓
开始跟弹
  ↓
实时提示
  ↓
完成歌曲
  ↓
展示准确率 + 星星
  ↓
检查新成就/新皮肤
  ↓
一键继续下一首
```

完成页只回答三个问题：

- 我弹完了吗？
- 我表现怎么样？
- 我解锁了什么？

不要出现大量统计图表。

---

# 三、视觉与 UX 设计系统

## 3.1 视觉核心

产品整体不要做成“儿童教育后台”，也不要做成“手游商城”。

建议定位：

> **儿童乐器玩具 × 轻游戏 × 极简音乐学习工具**

视觉特征：

- 大面积留白
- 大尺寸可触控元素
- 圆润但不过度卡通
- 少量高饱和重点色
- 大图形、小文字
- 明确层级
- 动效服务于反馈，不服务于炫技

## 3.2 色彩原则

基础 UI 使用稳定中性体系。

皮肤只覆盖“琴键与局部氛围”，不要让整个 App 每换一次皮肤就像换了一套产品。

### 基础层

```css
--app-bg
--surface
--surface-elevated
--text-primary
--text-secondary
--border
--accent
--success
--warning
```

### 皮肤层

只允许覆盖以下语义变量：

```css
--skin-key-bg
--skin-key-active
--skin-key-border
--skin-key-shadow
--skin-guide
--skin-stage-bg
--skin-decoration
```

禁止皮肤直接重写整个应用的 CSS。

## 3.3 动效原则

所有动效分三类：

### 输入反馈

50–150ms。

例如：琴键下沉、颜色变化、音符反馈。

### 状态反馈

300–700ms。

例如：完成、获得星星、皮肤解锁。

### 氛围动效

低频、慢速。

例如：森林树叶、星空微动。

禁止：

- 无限高速闪烁
- 大面积持续发光
- 高频粒子爆炸
- 强制自动播放复杂动画
- 关键按钮持续跳动制造点击压力

必须支持：

```css
@media (prefers-reduced-motion: reduce) {
  /* 降级动画 */
}
```

---

# 四、页面结构

## 4.1 首页

布局建议：

```text
┌──────────────────────────────────────────┐
│ Logo / 今日练习        ⭐ 12        ⚙    │
│                                          │
│          今天想怎么玩？                  │
│                                          │
│     ┌──────────┐  ┌──────────┐           │
│     │  跟我弹  │  │  自由弹  │           │
│     └──────────┘  └──────────┘           │
│                                          │
│     ┌──────────┐  ┌──────────┐           │
│     │ 小挑战   │  │ 我的装扮 │           │
│     └──────────┘  └──────────┘           │
│                                          │
│     最近练过：小星星 / 两只老虎          │
└──────────────────────────────────────────┘
```

首页不要做成复杂 dashboard。

## 4.2 练习页

横屏优先：

```text
┌────────────────────────────────────────────────┐
│ ← 歌曲       ▶ 听一遍      ♫ 速度      ●      │
├────────────────────────────────────────────────┤
│                                                │
│        当前音符： ● E4                         │
│        下一音： G4                             │
│                                                │
│ ┌────────────────────────────────────────────┐ │
│ │                 钢琴键盘                   │ │
│ └────────────────────────────────────────────┘ │
│                                                │
│  8键   15键   25键        录音       退出      │
└────────────────────────────────────────────────┘
```

注意：

- 键盘必须是页面主视觉
- 控件不能覆盖琴键
- 底部控制栏不超过一行
- iPad 横屏时优先保证键盘高度

## 4.3 完成页

```text
          完成啦！

        ★ ★ ★

       92% 准确率

     用时 01:32

   新获得：⭐ +3

   新解锁：星空琴键

   [ 再弹一次 ]   [ 下一首 ]
```

避免“考试报告”的视觉语言。

---

# 五、钢琴键盘系统 V3

## 5.1 三档键盘

### 8 键

目标：第一次接触钢琴。

特征：

- 全白键
- 音名明确
- 色彩辅助识别
- 不显示复杂乐理信息

### 15 键

目标：开始认识黑键和音域变化。

### 25 键

目标：接近真实钢琴的视觉结构。

## 5.2 键盘 DOM 结构

不要使用“白键和黑键全部 `display:flex`”的方式实现 15/25 键。

标准实现采用双层布局：

```text
Keyboard
├── WhiteKeysLayer
│   ├── C
│   ├── D
│   ├── E
│   └── ...
└── BlackKeysLayer
    ├── C#
    ├── D#
    └── ...
```

黑键通过绝对定位覆盖在白键层之上。

这样才能保证：

- 黑键位置正确
- 不因屏幕宽度改变相对关系
- 25 键可扩展
- 高亮状态不会破坏布局

## 5.3 Pointer Events

核心事件采用：

- `pointerdown`
- `pointerup`
- `pointercancel`
- `setPointerCapture()`

不要依赖 `pointerleave` 作为唯一释放方案。

`setPointerCapture()` 能保证拖出琴键边界后仍能收到后续 pointer 事件，适合触摸琴键场景。 citeturn782924search1turn782924search5

## 5.4 多点触控

必须支持至少两个同时按下的琴键。

状态模型：

```ts
Map<pointerId, note>
```

而不是单个：

```ts
activeKey: string | null
```

这样未来自然支持双手同时弹奏。

---

# 六、音频引擎架构 V3

## 6.1 设计原则

React 不直接管理 AudioNode。

建立独立的：

```text
AudioEngine
├── AudioContext
├── MasterGain
├── Compressor
├── Reverb
└── VoiceManager
```

React 只调用：

```ts
ensureReady()
noteOn(note, velocity)
noteOff(note)
allNotesOff()
setVolume(value)
```

## 6.2 AudioContext 初始化

必须在用户手势中创建或恢复 AudioContext。浏览器通常会限制没有用户交互时的自动音频播放；`AudioContext` 处于 suspended 时需要在用户手势中 `resume()`。 citeturn782924search0turn782924search2turn782924search4

推荐策略：

```text
第一次 pointerdown
  ↓
ensureReady()
  ↓
AudioContext running
  ↓
noteOn()
```

不要增加强制“点击开始”弹窗，除非目标浏览器实际测试发现必须增加。

## 6.3 MVP 音色

不用真实采样钢琴，先做轻量合成：

- 基础振荡器
- 谐波叠加
- ADSR / Hold
- 轻微滤波
- Master compressor
- 极短 room reverb

建议不要直接把“单三角波 + 单正弦波”定义为最终音色目标。它可以作为技术验证，但听感容易更像电子琴。

## 6.4 音色升级路线

```text
V1：自研轻量合成
  ↓
V2：Tone.js PolySynth（必要时）
  ↓
V3：高品质采样钢琴（按反馈决定）
```

只有当“音色质量”成为真实用户反馈中的核心问题时才升级。

## 6.5 Listen Mode 调度

不要使用每个音符一个 `setTimeout()` 的方式做长曲播放。

应该建立统一时间轴：

```ts
beat → seconds → AudioContext.currentTime
```

音频提前调度，视觉高亮使用同一时间轴同步。

这样可以显著降低：

- 长曲累积误差
- 后台切换后的不同步
- 音符高亮与声音错位

---

# 七、歌曲数据模型

## 7.1 Song

建议结构：

```ts
export interface Song {
  id: string;
  title: string;
  category: 'folk' | 'classic' | 'english' | 'festival';
  difficulty: 1 | 2 | 3;
  bpm: number;
  defaultKeyboard: 8 | 15 | 25;
  notes: SongNote[];
  transpositions?: SongTranspose[];
}
```

## 7.2 SongNote

```ts
export interface SongNote {
  note: string;
  beat: number;
  duration: number;
  hand?: 'left' | 'right';
  velocity?: number;
}
```

所有时间统一使用“拍”作为编辑单位，运行时再转换成秒。

## 7.3 不建议把“8键版本”和“25键版本”复制成两份曲谱

应保存一份原始旋律 + 键盘映射策略：

```text
原始曲谱
  ↓
KeyboardRangeResolver
  ↓
8键 / 15键 / 25键
```

这样未来调整键盘范围不会维护三份数据。

---

# 八、练习与评分系统

## 8.1 练习状态机

不要通过多个 boolean 组合状态。

使用明确状态：

```ts
type PracticeState =
  | 'idle'
  | 'listening'
  | 'playing'
  | 'paused'
  | 'completed'
  | 'result';
```

## 8.2 每次按键记录

```ts
interface PerformanceEvent {
  note: string;
  expectedNote?: string;
  actualAt: number;
  expectedAt?: number;
  timingDeltaMs?: number;
  correct: boolean;
}
```

录音、评分、复盘全部复用这一份事件数据。

## 8.3 准确率

建议拆成两个维度：

```text
Pitch Accuracy
Timing Accuracy
```

最终再合成为：

```text
accuracy = pitchScore * 0.7 + timingScore * 0.3
```

初学者采用宽松 timing window，随着难度增加再缩小窗口。

## 8.4 星星奖励

继续使用 1–3 星，但星星不被“消费”。

```text
< 70%    → +1
70–89%   → +2
≥ 90%    → +3
```

星星代表“成长累计值”。

例如：

```text
累计 10 ⭐ → 太空启蒙
累计 30 ⭐ → 糖果
累计 50 ⭐ → 星球
```

孩子获得的星星不会因为解锁皮肤而减少。

这样会比“花星星买皮肤”更符合儿童成长产品定位。

---

# 九、成长数据模型 V3

## 9.1 建议统一为一个 Progress Store

不再让 SkinContext、AchievementContext、PracticeContext 各自保存核心业务状态。

统一：

```ts
interface AppProgress {
  version: number;
  starsTotal: number;
  equippedSkinId: string;
  unlockedSkinIds: string[];

  completedSongIds: string[];
  bestAccuracyBySong: Record<string, number>;

  streak: {
    currentDays: number;
    longestDays: number;
    lastPracticeDate: string | null;
  };

  stats: {
    songsStarted: number;
    songsCompleted: number;
    totalNotesPlayed: number;
    bestAccuracy: number;
    adventureLevel: number;
    bestRhythmCombo: number;
  };

  achievements: string[];
}
```

## 9.2 localStorage

统一 key：

```text
piano_app_progress_v1
```

并增加：

```text
piano_app_settings_v1
```

两个数据域分开：

- progress：成长数据
- settings：音量、键盘档位、速度等偏好

## 9.3 必须支持迁移

未来：

```text
v1 → v2 → v3
```

不要直接覆盖旧数据。

---

# 十、皮肤系统 V3

## 10.1 命名

最终 UI 不使用：

- 商店
- 购买
- 商品
- 稀有度
- 价格

统一使用：

> **我的装扮**

或：

> **琴键衣柜**

## 10.2 皮肤不再直接保存 CSS

V2 中 JSON 直接保存：

```json
"--key-bg": "linear-gradient(...)"
```

虽然可以运行，但长期维护成本较高。

推荐改成：

```json
{
  "id": "space",
  "name": "太空",
  "themeId": "space",
  "previewEmoji": "🪐"
}
```

然后由 TypeScript/CSS Theme Registry 提供主题：

```ts
const skinThemes = {
  rainbow: {...},
  star: {...},
  fire: {...},
  crystal: {...},
  forest: {...},
  neon: {...}
};
```

优点：

- 类型更安全
- 不允许任意字符串直接写进 style
- 设计系统统一
- 修改皮肤不会污染数据层
- AI 后续添加皮肤时更稳定

## 10.3 Skin 类型

```ts
export interface Skin {
  id: string;
  name: string;
  description: string;
  themeId: string;
  unlock: UnlockCondition;
  previewEmoji: string;
  decorations?: string[];
}
```

删除儿童界面的 `rarity`。

可以在内部保留 `collectionGroup`，但不展示“普通/稀有/传奇”竞争性标签。

## 10.4 解锁条件

```ts
type UnlockCondition =
  | { type: 'default' }
  | { type: 'stars'; count: number }
  | { type: 'songCompleted'; songId: string }
  | { type: 'streak'; days: number }
  | { type: 'accuracy'; threshold: number }
  | { type: 'adventure'; level: number }
  | { type: 'rhythmCombo'; combo: number }
  | { type: 'persistence'; achievementId: string }
  | { type: 'seasonal'; eventId: string };
```

## 10.5 解锁检测必须原子化

V2 最大的代码问题之一是：

```ts
addStars()
updateStats()
checkUnlocks()
```

这些 React state 更新是异步的，`checkUnlocks()` 可能拿到更新前的旧 progress，导致刚完成一首歌后皮肤不是立刻解锁，而是在下一次状态变化后才出现。

V3 建议只提供一个业务入口：

```ts
recordPracticeResult(result)
```

内部一次性完成：

```text
计算星星
↓
更新歌曲记录
↓
更新最佳成绩
↓
更新连续练习
↓
检测普通解锁
↓
检测成就解锁
↓
生成 RewardEvent
↓
一次性提交新的 Progress
```

返回：

```ts
interface RewardResult {
  starsEarned: number;
  newlyUnlockedSkinIds: string[];
  newlyUnlockedAchievementIds: string[];
}
```

这样完成页直接使用返回结果，不依赖 React state 是否已经刷新。

## 10.6 特定曲目解锁必须精确判断

V2：

```ts
case 'song':
  return progress.stats.songsCompleted > 0;
```

这会导致“完成任何歌曲”都解锁指定歌曲皮肤。

应该判断：

```ts
progress.completedSongIds.includes(u.songId)
```

## 10.7 连续练习不能简单 `streakDays += 1`

必须保存：

```ts
lastPracticeDate
```

然后按本地自然日判断：

```text
昨天练过 → currentDays + 1
今天已经练过 → 不重复增加
超过一天没练 → 重置为 1
```

## 10.8 皮肤预览

预览不修改正式装备状态。

```text
currentSkinId
previewSkinId
```

渲染时：

```ts
activeSkin = previewSkinId ?? currentSkinId
```

关闭预览后立即恢复。

## 10.9 试弹

预览窗必须是真正的交互式琴键，而不是静态三个 demo 方块。

体验：

```text
打开皮肤
  ↓
看到真实 8/15/25 键缩略键盘
  ↓
可以试弹
  ↓
看到按下/高亮效果
  ↓
已解锁 → 装备
未解锁 → 查看条件
```

## 10.10 解锁节奏

建议：

### 第一层

第一次完整弹奏即可获得明显反馈。

### 第二层

10–15 ⭐ 区间出现第一个累计成长皮肤。

### 第三层

连续练习、准确率、特殊坚持行为触发不同方向的皮肤。

### 长期

30–50 ⭐ + 冒险 / 节拍挑战 / 隐藏成就。

不建议让“节日限定”成为主成长路径。

---

# 十一、皮肤视觉系统

## 11.1 皮肤类型

MVP：

1. 彩虹
2. 星空
3. 森林
4. 糖果
5. 水晶

第二阶段：

6. 火焰
7. 太空
8. 霓虹
9. 勇气
10. 节日主题

## 11.2 皮肤只影响三层

### Layer 1：琴键

- 底色
- 按下态
- 边框
- 阴影

### Layer 2：引导

- 当前目标键
- 下一个音符
- 完成反馈

### Layer 3：舞台氛围

- 背景
- 少量装饰

核心菜单、按钮、文字系统保持稳定。

---

# 十二、互动模式规划

## 12.1 自由弹

最简单。

无评分、无失败。

核心目标：让孩子随便按。

## 12.2 动物模式

重点不是“逼真动物音”，而是建立：

> 按键 → 声音 → 想象

MVP 可以使用 Web Audio 程序化效果。

如果追求更真实的动物音，再增加极小型本地音频资源。

## 12.3 打鼓模式

8 个区域映射：

```text
Kick
Snare
Hat
Clap
Tom
Low Tom
Crash
Percussion
```

使用独立 `DrumEngine`，不要把鼓音逻辑塞进 PianoEngine。

## 12.4 节拍挑战

核心：

```text
时间轴 → 目标区 → 用户按键 → 判断命中
```

不要一开始做成复杂“劲舞团”系统。

第一版只需要：

- 速度
- marker
- hit window
- combo
- result

## 12.5 音乐森林冒险

这是内容工作量最大的模块。

不要把“20 章 / 60 关”直接作为研发硬目标。

应拆成：

```text
系统能力：1 套
关卡模板：6–8 种
首发内容：12 关
后续内容：48 关
```

否则会出现程序已经完成、内容却没有制作能力匹配的问题。

关卡应该由数据驱动：

```ts
interface ChallengeLevel {
  id: string;
  chapter: number;
  type: ChallengeType;
  target: unknown;
  difficulty: 1 | 2 | 3;
  reward?: string;
}
```

---

# 十三、录音系统

## 13.1 不录制 AudioStream

继续保持：

> 录的是音符事件，不是麦克风/系统音频。

优点：

- 文件极小
- 可重放
- 可重新评分
- 可查看错误
- iOS 环境更容易控制

## 13.2 Replay

```ts
interface Recording {
  id: string;
  songId?: string;
  createdAt: number;
  events: PerformanceEvent[];
}
```

播放时重新发送：

```text
Recording → AudioEngine
```

---

# 十四、PWA 与离线架构

“离线优先”是产品核心，不应等到第四阶段才实现。

推荐在 MVP 后半段就加入：

```text
App Shell
↓
Service Worker
↓
Static Asset Cache
↓
LocalStorage
↓
完全离线继续使用
```

离线缓存至少包括：

- JS
- CSS
- SVG
- 曲谱 JSON
- 皮肤数据
- 必要的短音频资源

如果采用 TTS 旁白，不要把运行时 TTS 作为依赖。

`edge-tts` 实际上调用 Microsoft Edge 的在线 TTS 服务，所以不应把它描述成浏览器运行时的“离线 TTS”。更合理的方式是：开发/内容制作阶段用 TTS 生成文件，最终把音频作为静态资源打包进应用。 citeturn782924search3turn782924search7

---

# 十五、建议目录结构

```text
src/
├── app/
│   ├── App.tsx
│   ├── routes.ts
│   └── providers.tsx
│
├── features/
│   ├── piano/
│   │   ├── components/
│   │   ├── pianoLayout.ts
│   │   └── piano.css
│   │
│   ├── practice/
│   │   ├── PracticePage.tsx
│   │   ├── practiceReducer.ts
│   │   ├── scoring.ts
│   │   └── scheduler.ts
│   │
│   ├── songs/
│   │   ├── songData.ts
│   │   └── songResolver.ts
│   │
│   ├── skins/
│   │   ├── SkinWardrobe.tsx
│   │   ├── skinThemes.ts
│   │   ├── skins.ts
│   │   └── skinResolver.ts
│   │
│   ├── achievements/
│   │   └── achievements.ts
│   │
│   └── challenges/
│       ├── rhythm/
│       ├── animals/
│       ├── drums/
│       └── forest/
│
├── core/
│   ├── audio/
│   │   ├── AudioEngine.ts
│   │   ├── PianoSynth.ts
│   │   ├── DrumEngine.ts
│   │   └── Reverb.ts
│   │
│   ├── progress/
│   │   ├── ProgressStore.ts
│   │   ├── migrations.ts
│   │   └── storage.ts
│   │
│   └── events/
│       └── rewardEvents.ts
│
├── data/
│   ├── songs.json
│   ├── skins.json
│   └── challenges.json
│
└── shared/
    ├── ui/
    ├── hooks/
    └── utils/
```

原则：

> **音频是核心服务，成长是核心数据，皮肤是 feature，不允许成为全局业务中心。**

---

# 十六、技术选型最终建议

| 项目 | V3 建议 |
|---|---|
| 构建 | Vite |
| UI | React + TypeScript |
| 样式 | CSS Modules / 原生 CSS + CSS Variables |
| 音频 | Web Audio API |
| 高级音频 | Tone.js（按需） |
| 数据 | localStorage |
| PWA | Service Worker |
| 部署 | Cloudflare Pages |
| 图形 | CSS / SVG 为主 |
| 状态 | React Context + useReducer / 单一 Progress Store |
| 测试 | Vitest + Playwright |
| 曲谱 | JSON / TypeScript 数据 |
| 音频转谱 | 后续独立功能 |

不建议 MVP 引入：

- Redux
- Zustand
- 后端数据库
- 登录系统
- CMS
- GraphQL
- 实时同步
- AI Agent

原因不是这些工具不好，而是当前产品不需要它们。

---

# 十七、开发里程碑 V3

## Phase 0：技术验证

目标：先证明“在 iPad Safari 上弹琴真的舒服”。

完成：

- AudioContext 初始化
- 8 键琴
- pointer events
- 多点触控
- 真实音频延迟测试
- 横屏布局

验收标准：

> 连续快速点击、快速滑动、两个手指同时按键，都不会卡音、重复触发或出现“卡住的音”。

## Phase 1：MVP

完成：

- 首页
- 8 键
- 3 首歌曲
- 听一遍
- 跟弹
- 高亮
- 完成评分
- 星星
- localStorage
- PWA 基础离线
- 3 套皮肤

## Phase 2：核心版本

完成：

- 15 / 25 键
- 25 首歌曲
- 移调
- 录音回放
- 勋章
- 5–10 套皮肤
- 完整装扮页面
- 练习连续天数

## Phase 3：游戏化

完成：

- 动物模式
- 打鼓模式
- 节拍挑战
- 第一版森林冒险 12 关
- 皮肤联动

## Phase 4：长期扩展

再考虑：

- 60 关完整森林冒险
- MIDI 外接
- 音频转谱
- 更多节日主题
- 分享录音/曲谱

---

# 十八、性能指标

建立明确预算，而不是只追求“bundle 100KB”。

目标：

- 首屏不加载大音频
- 核心 JS 保持轻量
- 所有皮肤无需额外大图
- 单个背景资源优先 SVG / WebP
- 按需加载非核心互动模块
- 页面打开后先能立即点击琴键

关键指标：

```text
Tap → Note On：尽量 < 50ms 主观感知目标
首屏 → 可弹：不等待曲谱或皮肤资源
核心练习页：不因装扮模块加载而阻塞
```

注意：以上是产品工程目标，不应在尚未测试前宣称已经达到。

---

# 十九、测试策略

## 19.1 音频测试

必须测试：

- iPad Safari
- iPhone Safari
- Chrome Desktop
- Edge Desktop

重点：

- 首次点击
- 快速连续点击
- 长按
- 多点触控
- 切后台再回来
- 锁屏再回来
- 静音状态

## 19.2 交互测试

必须覆盖：

```text
pointerdown
pointerup
pointercancel
release
multi-touch
orientation change
```

## 19.3 解锁系统测试

至少覆盖：

- 星星阈值刚好达到
- 超过阈值
- 已解锁重复检查
- 指定歌曲条件
- 连续天数
- 准确率最高成绩
- 隐藏成就
- 事件皮肤
- localStorage 数据损坏
- 版本迁移

## 19.4 E2E 核心路径

```text
打开网页
→ 按第一个键
→ 选择小星星
→ 听一遍
→ 开始跟弹
→ 完成
→ 获得星星
→ 皮肤解锁
→ 进入装扮
→ 预览
→ 试弹
→ 装备
→ 返回练习
```

这条路径必须作为每次发布前的冒烟测试。

---

# 二十、V2 代码中必须修正的问题清单

## 20.1 songsCompleted 会被覆盖

V2：

```ts
updateStats({ songsCompleted: 1 })
```

这会让完成歌曲数量长期停留在 1。

正确思路：

```ts
songsCompleted: prev.stats.songsCompleted + 1
```

但 V3 更建议统一进入 `recordPracticeResult()`。

## 20.2 bestAccuracy 更新逻辑错误

V2：

```ts
bestAccuracy: Math.max(accuracy, 0)
```

这不是“历史最高成绩”。

应该比较历史值：

```ts
bestAccuracy: Math.max(prev.stats.bestAccuracy, accuracy)
```

或针对单曲保存：

```ts
bestAccuracyBySong[songId]
```

## 20.3 checkUnlocks 存在 stale state 风险

原因：

```ts
addStars()
updateStats()
checkUnlocks()
```

在一次事件循环中，后面的 `checkUnlocks()` 可能仍读到旧 progress。

必须改成事务式更新。

## 20.4 song 解锁条件错误

必须使用：

```ts
completedSongIds.includes(songId)
```

而不是：

```ts
songsCompleted > 0
```

## 20.5 persistence / event 不应永久 `return false`

建议把特殊解锁统一抽象成：

```ts
achievements
```

业务完成一个成就后：

```ts
achievements.add('complete-after-mistake')
```

皮肤系统只负责检查成就是否满足。

这样皮肤模块无需知道“为什么达成”。

---

# 二十一、儿童体验红线

永远不加入：

- 广告
- 付费按钮
- 充值
- 抽卡
- 随机奖励箱
- 排行榜
- 社交攀比
- 强制连续签到
- “今天不练就失去奖励”
- 无限倒计时压力

允许加入：

- 星星
- 勋章
- 新装扮
- 完成动画
- 轻量挑战
- 连续练习记录

核心判断：

> **奖励应该告诉孩子“你刚刚完成了一件事”，而不是告诉孩子“你还差多少才值得开心”。**

---

# 二十二、V3 最终产品模型

整个应用最终收敛成五层：

```text
                 ┌───────────────┐
                 │   CONTENT     │
                 │ 歌曲 / 关卡   │
                 └───────┬───────┘
                         ↓
                 ┌───────────────┐
                 │   PRACTICE    │
                 │ 跟弹 / 自由弹 │
                 └───────┬───────┘
                         ↓
                 ┌───────────────┐
                 │     INPUT     │
                 │ Pointer/MIDI  │
                 └───────┬───────┘
                         ↓
                 ┌───────────────┐
                 │     AUDIO     │
                 │ Web Audio API │
                 └───────────────┘

                        ↑
                        │
                 ┌──────┴───────┐
                 │   PROGRESS   │
                 │ 星星/成就/记录 │
                 └──────┬───────┘
                        ↓
                 ┌───────────────┐
                 │   COSMETIC    │
                 │    皮肤装扮   │
                 └───────────────┘
```

最终产品不是“儿童版钢琴商城”，而是：

> **一个极低门槛的网页钢琴玩具 + 一个轻量音乐启蒙系统 + 一个不消费、只成长的装扮奖励层。**

---

# 二十三、给 AI 编程模型的最高优先级开发规则

1. 不改变产品核心定位。
2. 不为了“可扩展”提前增加后端、复杂状态管理或 AI Agent。
3. 不把皮肤系统做成商城逻辑。
4. 不允许组件直接修改 localStorage。
5. 所有成长数据通过 ProgressStore 修改。
6. 所有音频通过 AudioEngine 修改。
7. React 不直接持有 AudioNode 生命周期。
8. PianoKeyboard 不负责评分。
9. Skin 不负责练习业务。
10. Challenge 不直接修改 Skin 数据。
11. 皮肤只能通过 Progress 事件解锁。
12. 视觉皮肤只覆盖语义 CSS Tokens。
13. 非核心模块不得阻塞首次弹琴。
14. 所有 touch / pointer 行为必须在真实 iPad 上验证。
15. 功能增加前先确认是否真正服务“弹琴启蒙”。
16. 优先修正确性、稳定性、触感和声音，再增加视觉效果。
17. 不使用复杂动画掩盖交互问题。
18. 不在儿童主流程中加入任何付费、广告、随机奖励或攀比机制。

---

# 二十四、V3 开发验收标准

当以下体验全部成立，才认为 MVP 真正完成：

```text
打开网页
↓
无需说明即可按第一个琴键
↓
第一声琴声快速出现
↓
孩子知道下一步做什么
↓
能够完整弹完一首简单歌曲
↓
完成后立即获得明确反馈
↓
可以看到成长
↓
可以预览并试弹新皮肤
↓
回到钢琴继续玩
```

最终判断标准不是“功能有没有做完”，而是：

> **第一次接触这个网页的孩子，是否可以在不看教程的情况下，30 秒内成功弹出一段声音，并自然进入第一次完整练习。**
